# Requirement Traceability

Lightweight, living map: **Requirement → Specification → Implementation → Test.** Updated as implementation proceeds (per session, per task) — not a one-time artifact. Kept intentionally simple: a Markdown table, not a tracking system. Use the `requirement-traceability` skill when updating this file.

Status: **Phase 5A = infrastructure/bootstrap only** (Rails/React/PostgreSQL/Docker Compose runnable foundation, no business logic). **Phase 5B.2 = persistence foundation** (models, migrations, constraints, seed data — no business APIs; not yet reflected row-by-row below). **Phase 5B.3 = first real business API**: guest join + idempotency (REQ-GUEST-001, REQ-GUEST-007 below). **Phase 5B.4 = guest current-visit/status + informational position** (REQ-GUEST-002, REQ-GUEST-004 below — the *full* DEC-005 position computation remains deferred to the allocation service). **Phase 5B.5.1 = allocation algorithm locked** (specification/analysis only). **Phase 5B.5.2 = pure allocation decision engine implemented** (`backend/app/services/allocation/decision_engine.rb` and friends — no database mutation). **Phase 5B.5.3 = transactional allocation implemented** (`Allocation::ReservationService` — real table locking, atomic 1–2-table reservation, `seating_code` generation, `waiting → ready`, verified under real PostgreSQL concurrency). **Phase 5B.5.4 = trigger integration + repeated allocation orchestrator** (`Allocation::Orchestrator`) — Guest Join and the lazy READY-expiration no-show path now actually invoke allocation end-to-end; Release and Guest Leave remain unintegrated because neither has an implemented code path to attach to yet (not a specification gap — see `allocation-algorithm.md` §25c). **Phase 5B.6 = staff seating confirmation** (`Staff::ConfirmSeatingService`) — `POST /staff/seat` implemented and verified under real concurrency; deliberately **unauthenticated** at the time (no staff login/session mechanism existed yet). **First frontend vertical slice = guest join screen** (`frontend/src/App.tsx`) — a real browser now drives the real `POST /guest/queue-entries` → `GET /guest/queue-entries/current` flow against the real backend/database. **Guest Leave** (`Guest::LeaveService`, plus a "Leave Queue" button in the same frontend screen) — the fourth and final approved allocation trigger (Phase 5B.5.4's own deferred item) is now integrated; real end-to-end verified including a released table being allocated to another waiting guest. **Staff Login** (`Staff::LoginService`/`SessionToken`) — a P0 stub login issuing a signed bearer token; `POST /staff/seat` now requires it (closing Phase 5B.6's own deferred gap); a minimal Staff Login screen exists in the same frontend app. **Staff Queue** (`Staff::QueueViewService`) — `GET /staff/queue` implemented as a read-only view (waiting + ready lists), never allocates; DEC-015's lazy-expiration check was extracted into a shared `Allocation::LazyExpiration` module (now used by three call sites) rather than duplicated a third time; a Staff Queue screen exists in the frontend, shown after Staff Login. **Frontend session persistence** (Phase 5B.9) — both the guest `active_visit_token` and the staff session token now survive a browser refresh via `localStorage` (separate keys, no backend contract changed); the guest visit is re-validated against the real backend on load, and the staff session is validated by the Staff Queue's own real fetch (a `401` clears it). **Staff Tables** (Phase 5B.10, `Staff::TableViewService`) — `GET /staff/tables` implemented as a read-only view (`free`/`held`/`occupied` per table, derived, no `tables.status` column), never allocates; a Queue/Tables toggle in the frontend reveals it alongside Staff Queue. **Staff Seat UI + Release + No-show** (Phase 5B.11) — the missing Seat Guest frontend was added for the already-existing `POST /staff/seat`; `Staff::ReleaseService`/`Staff::NoShowService` (new) implement `POST /staff/seating-assignments/release` and `POST /staff/queue/no-show`, both entry-only, both triggering `Allocation::Orchestrator` after a real release; frontend Release/No-show actions added to Staff Tables/Queue. All three P0 Staff operational capabilities and their frontends now exist. **Oversized-group rejection** (Phase 5B.12, DEC-011) — a guest join whose `group_size` exceeds the structural maximum of every valid table configuration (all tables, not just currently-free ones) is now rejected before any `QueueEntry` is created, closing the deferral noted since Phase 5B.3; a group that merely can't be seated *right now* still joins normally and stays `waiting`. **Guest dynamic position** (Phase 5B.13, DEC-005) — the guest-facing `position` (and Staff Queue's identical value for the same entry) now reflects the real allocation policy (compatibility, current availability, aging, starvation), computed via `Allocation::QueuePositionCalculator` reusing `Allocation::DecisionEngine` unmodified, purely in memory — closing the "chronological rank only" simplification documented since Phase 5B.4. All other business requirements remain `—` until their own phase implements them. This file should be updated in the same commit/checkpoint as the code and tests it describes, not after the fact from memory.

## How to use this file

- When starting an implementation task, find the requirement ID(s) it covers below and confirm the "Specification" column points to what you're about to build.
- When the task is done, fill in "Implementation" (file/module) and "Test" (test ID from `05-specifications/test-strategy.md`, or file path) for each requirement it satisfies.
- If a requirement has no realistic test (e.g., a UI copy detail), write `manual` in the Test column with a one-line note, not a blank.
- Never mark a requirement done in this file without a corresponding test or an explicit, noted reason one isn't applicable.

## P0 requirements

| Requirement | Specification | Implementation | Test |
|---|---|---|---|
| REQ-GUEST-001 (join) | `05-specifications/functional-spec.md` §1, `api-spec.md` join | `backend/app/services/guest/join_service.rb`, `backend/app/controllers/guest/queue_entries_controller.rb` — entry creation, `active_visit_token`; a new join triggers `Allocation::Orchestrator` and may synchronously return `status: "ready"` (see `api-spec.md` join "Implementation status"); an oversized/impossible `group_size` is rejected before creation (DEC-011, Phase 5B.12). **`position` is permanently not part of this response** (not deferred — dynamic position now fully exists, Phase 5B.13, but only via `GET /guest/queue-entries/current`, by design — see `api-spec.md` join "Implementation status") | `backend/test/services/guest/join_service_test.rb`, `backend/test/controllers/guest/queue_entries_controller_test.rb`, `backend/test/controllers/guest/queue_entries_allocation_integration_test.rb` |
| REQ-GUEST-002 (view position) | `functional-spec.md` §9, `api-spec.md` current | `backend/app/services/guest/current_queue_status_service.rb`, `backend/app/services/allocation/queue_position_calculator.rb` (new) — **the full DEC-005 (table-availability/compatibility/aging/starvation-aware) position is now implemented** (Phase 5B.13), reusing `Allocation::DecisionEngine` as a pure in-memory simulation — see `api-spec.md` current "Position semantics" | `backend/test/services/guest/current_queue_status_service_test.rb`, `backend/test/controllers/guest/current_queue_status_controller_test.rb`, `backend/test/services/allocation/queue_position_calculator_test.rb` |
| REQ-GUEST-003 (leave) | `functional-spec.md` §3, `api-spec.md` leave | `backend/app/services/guest/leave_service.rb`, `backend/app/controllers/guest/queue_entries_controller.rb#leave` — `waiting`/`ready → left`, atomic release + conditional `Allocation::Orchestrator` trigger, real end-to-end verified (frontend "Leave Queue" button too, `frontend/src/App.tsx`) | `backend/test/services/guest/leave_service_test.rb`, `backend/test/controllers/guest/queue_entries_leave_controller_test.rb` |
| REQ-GUEST-004 (recover visit) | `functional-spec.md` §2, DEC-006 | `backend/app/services/guest/current_queue_status_service.rb`, `backend/app/controllers/guest/queue_entries_controller.rb#current` — token-based lookup, all four state shapes (`waiting`/`ready`/terminal/unknown); **frontend now persists `active_visit_token` in `localStorage` (`frontend/src/App.tsx`, Phase 5B.9) and recovers the visit from the real backend on page load/refresh** — a 404 clears the stored token, a network/5xx error keeps it and offers Retry, never fabricated locally | `backend/test/services/guest/current_queue_status_service_test.rb`, `backend/test/controllers/guest/current_queue_status_controller_test.rb`; TEST-013, TEST-014; manual — real-browser refresh verification (no frontend test framework exists) |
| REQ-GUEST-005 (seating code) | `api-spec.md` current | `backend/app/services/allocation/seating_code_generator.rb` — generated exactly once, at `waiting → ready` allocation time (`Allocation::ReservationService`), returned via `GET /guest/queue-entries/current` while `ready`, never regenerated | `backend/test/services/allocation/reservation_service_test.rb`, `backend/test/controllers/guest/current_queue_status_controller_test.rb` |
| REQ-GUEST-006 (no cross-visit history) | DEC-006 | Satisfied by design, not by a specific file — no account/login/history feature exists anywhere in this codebase; each visit is an unrelated anonymous `active_visit_token` tied to one `QueueEntry` (`backend/app/models/queue_entry.rb`) | — (absence of a feature; nothing to test) |
| REQ-GUEST-007 (double-join protection) | `functional-spec.md` §1, DEC-007 | `backend/app/services/guest/join_service.rb` — idempotency key resolution (create / replay / conflict), backed solely by the DB unique index on `idempotency_key` (CORR-005) | `backend/test/services/guest/join_service_test.rb`, `backend/test/services/guest/join_service_concurrency_test.rb` (real-thread concurrency), `backend/test/controllers/guest/queue_entries_controller_test.rb`; TEST-002, TEST-003, TEST-021 |
| REQ-STAFF-001 (login) | `functional-spec.md` §4, `api-spec.md` login | `backend/app/services/staff/{login_service,session_token}.rb`, `backend/app/controllers/staff/{login_controller,base_controller}.rb` — stub-strength credential check, signed bearer token, no user enumeration; `POST /staff/seat` now requires it; frontend Staff Login screen (`frontend/src/App.tsx`) | `backend/test/services/staff/login_service_test.rb`, `backend/test/controllers/staff/login_controller_test.rb`, plus new auth-isolation tests in `seat_controller_test.rb` |
| REQ-STAFF-002 (view queue) | `functional-spec.md` §5, `api-spec.md` `GET /staff/queue` | `backend/app/services/staff/queue_view_service.rb`, `backend/app/controllers/staff/queue_controller.rb` — read-only `{ waiting: [...], ready: [...] }`, DEC-015 lazy expiration via the now-shared `Allocation::LazyExpiration` module (never triggers `Allocation::Orchestrator`, per this phase's own explicit "must not perform new allocation"); frontend Staff Queue screen (`frontend/src/App.tsx`) shown after Staff Login | `backend/test/services/staff/queue_view_service_test.rb`, `backend/test/controllers/staff/queue_controller_test.rb` |
| REQ-STAFF-003 (view tables) | `functional-spec.md` §5, `api-spec.md` `GET /staff/tables` | `backend/app/services/staff/table_view_service.rb`, `backend/app/controllers/staff/table_controller.rb` — read-only `{ tables: [...] }`, state (`free`/`held`/`occupied`) derived from `SeatingAssignmentTable`/`SeatingAssignment`, no `tables.status` column; DEC-015 lazy expiration via the now-four-call-site `Allocation::LazyExpiration` module (never triggers `Allocation::Orchestrator`); frontend Staff Tables screen (`frontend/src/App.tsx`), a Queue/Tables toggle alongside Staff Queue | `backend/test/services/staff/table_view_service_test.rb`, `backend/test/controllers/staff/table_controller_test.rb` |
| REQ-STAFF-004 (seat by code) | `functional-spec.md` §6, `api-spec.md` seat | `backend/app/services/staff/confirm_seating_service.rb`, `backend/app/controllers/staff/seat_controller.rb` — `seating_code` lookup, atomic `ready`+`pending → seated`+`active`, verified under real PostgreSQL concurrency (Phase 5B.6); **now requires staff authentication** (Phase 5B.7 — see `api-spec.md` seat "Implementation status"); **frontend Seat Guest UI completed** (Phase 5B.11 — standalone code entry + per-row Seat button in Staff Queue, `frontend/src/App.tsx`) | TEST-004, TEST-005, TEST-006, TEST-015, plus `confirm_seating_service_test.rb`, `confirm_seating_service_concurrency_test.rb`, `seat_controller_test.rb` |
| REQ-STAFF-005 (release) | `functional-spec.md` §7, DEC-014 | `backend/app/services/staff/release_service.rb`, `backend/app/controllers/staff/release_controller.rb` — entry-only lookup (never `table_id`), atomic release of 1–2 `SeatingAssignmentTable` rows, `Allocation::Orchestrator` triggered after commit; `QueueEntry.status` deliberately unchanged (stays `seated`, per `domain-model.md` §2); frontend "Release" action on `occupied` Staff Tables rows | TEST-008, TEST-026, plus `release_service_test.rb`, `release_controller_test.rb` |
| REQ-STAFF-006 (no-show) | `functional-spec.md` §8 | `backend/app/services/staff/no_show_service.rb`, `backend/app/controllers/staff/no_show_controller.rb` — `waiting`/`ready → no_show`, atomic release of a `ready` entry's assignment, `Allocation::Orchestrator` triggered after commit; frontend "No-show" action on `waiting`/`ready` Staff Queue rows | TEST-012, plus `no_show_service_test.rb`, `no_show_controller_test.rb` |
| REQ-STAFF-007 (no camera scanner) | — (product scope note only) | Satisfied by design — no camera/QR scanning code exists anywhere in this codebase; seating codes are entered manually via the Staff Seat Guest UI | — (absence of a feature; nothing to test) |
| REQ-STAFF-008 (single staff UI, safe backend writes) | `architecture.md` §3 | `frontend/src/App.tsx`'s single `StaffLogin`-rooted screen (Queue/Tables toggle, Seat/Release/No-show actions) drives every staff write through the real, authenticated `Staff::*` service objects — no direct DB access, no bypass path | TEST-001, TEST-004, plus every `staff/*_controller_test.rb` file's own authentication-boundary tests |
| REQ-TABLE-001 (seed data) | DEC-001, `data-model.md` | `backend/db/seeds.rb`, `backend/db/migrate/20260816102233_create_tables.rb` — deterministic 40-table seed (20×2/18×4/2×6 capacity) | TEST-016, `backend/test/models/seed_data_test.rb` |
| REQ-TABLE-002/003 (exclusivity) | `domain-model.md` INV-001–003 | `backend/db/migrate/20260816102237_create_seating_assignment_tables.rb` — partial unique index on `table_id WHERE released_at IS NULL` (INV-016's own "self-contained constraint" lesson, CORR-004), plus `SeatingAssignmentTable#table_not_already_claimed` as a friendlier Rails-level check | TEST-004, TEST-007, `backend/test/models/seating_assignment_table_test.rb` |
| REQ-TABLE-004 (smaller group, larger table) | `allocation-spec.md` §1, `allocation-algorithm.md` §6 (`fit_score`) | `backend/app/services/allocation/decision_engine.rb`, now reachable end-to-end via `Allocation::Orchestrator` on Guest Join and lazy no-show expiration (Phase 5B.5.4) | TEST-023, `decision_engine_test.rb`, `orchestrator_test.rb` |
| REQ-TABLE-005 (max two-table combination) | DEC-002 | `backend/app/models/seating_assignment_table.rb#assignment_has_at_most_two_tables`; `Allocation::TableConfiguration.combined`/`ConfigurationGenerator` never generate a 3+ table configuration | TEST-006, `backend/test/models/seating_assignment_table_test.rb`, `backend/test/services/allocation/configuration_generator_test.rb` |
| REQ-TABLE-006 (atomic combined allocation) | `allocation-spec.md` §5, `allocation-algorithm.md` §18, INV-005 | `backend/app/services/allocation/reservation_service.rb`, now reachable end-to-end via `Allocation::Orchestrator` (Phase 5B.5.4) | TEST-005, TEST-006, `reservation_service_test.rb`/`reservation_service_concurrency_test.rb`, `orchestrator_test.rb` |
| REQ-TABLE-007/008 (combination lifecycle) | INV-004, INV-006 | `SeatingAssignmentTable#released_at` (never deleted, set atomically together for a combined pair) across every release path — `Staff::ReleaseService`, `Staff::NoShowService`, `Guest::LeaveService`, `Allocation::LazyExpiration` | TEST-022, TEST-008, plus `release_service_test.rb`'s combined-release tests |
| REQ-QUEUE-001/002 (not FIFO, position reflects availability) | `seating-allocation-policy.md`, `allocation-algorithm.md` (full document) | `backend/app/services/allocation/{decision_engine,reservation_service,orchestrator}.rb` — all four approved triggers now integrated end-to-end: Guest Join and lazy no-show expiration (Phase 5B.5.4), Guest Leave (Phase 5B.7), Staff Release and Staff No-show (Phase 5B.11) | TEST-011, TEST-024, `orchestrator_test.rb`, `queue_entries_allocation_integration_test.rb`, `release_service_test.rb`, `no_show_service_test.rb`, `leave_service_test.rb` |
| REQ-QUEUE-003 (starvation protection) | `starvation-policy.md`, `allocation-algorithm.md` §9 | `backend/app/services/allocation/decision_engine.rb`, verified effective through the full orchestrated flow (Phase 5B.5.4) | TEST-009, TEST-010, `orchestrator_test.rb` |
| REQ-QUEUE-004 (dynamic position) | DEC-005, `allocation-algorithm.md` §11 (`total_score`) | **Guest-facing `position` now exposes the real `total_score`-based ranking** (Phase 5B.13, `Allocation::QueuePositionCalculator`) — no longer a chronological approximation; Staff Queue reports the identical value for the same entry | TEST-011, plus `queue_position_calculator_test.rb` |
| REQ-INFRA-001/002 (persistence, migrations) | `data-model.md` | `backend/db/migrate/*` (6 migrations, Phase 5B.2), `backend/db/schema.rb` — PostgreSQL via Docker Compose, real migrations, no manual schema edits | TEST-016 |
| REQ-INFRA-003 (hard-path tests) | `test-strategy.md` | — | (this table) |
| REQ-INFRA-004 (runnable, Docker Compose) | `architecture.md` §6 | — | manual — `docker compose up` smoke test |
| REQ-FE-001–006 (frontend) | `functional-requirements.md` | `frontend/src/App.tsx` — **guest join/leave screen + Staff Login + Staff Queue, both sessions now persisted across a browser refresh (Phase 5B.9)**: guest form → `POST /guest/queue-entries` → `GET /guest/queue-entries/current` → WAITING+position or READY+seating_code, with "Leave Queue"; `active_visit_token` persisted in `localStorage` (`guest_active_visit_token`) and re-validated against the real backend on load; a simple Guest/Staff toggle (no router, no new dependency) reveals a separate email+password form → `POST /staff/login` → on success, a read-only Staff Queue view (`GET /staff/queue`, fetched on mount and via a manual "Refresh" button — no live updates, P1); the staff session token is likewise persisted (`staff_session_token`, plus a non-sensitive `staff_session_email` for display), optimistically restored on load and actually validated by the Staff Queue's/Staff Tables' own real fetch (a `401` there clears the stored session); a Queue/Tables toggle inside the authenticated Staff screen reveals a read-only Staff Tables view (`GET /staff/tables`); a standalone "Seat Guest" panel plus per-row Seat/No-show (Staff Queue) and Release (Staff Tables, on `occupied` rows) actions complete the staff operational flow — every action re-fetches Queue/Tables from the real backend afterward (a shared `refreshSignal`, no state-management library) | manual — real-browser end-to-end verification (no frontend test framework exists in this project yet); TEST-003 (double-join, backend-level) |
| DEC-011 (oversized-group rejection) | `functional-spec.md` §1, `allocation-spec.md` §0 | `backend/app/services/guest/join_service.rb` (`group_size_too_large?`), `backend/app/services/allocation/configuration_generator.rb` (`self.maximum_seatable_group_size`, new) — rejected before `QueueEntry` creation, checked against the structural maximum (all tables, not just free ones); currently-unavailable-but-valid groups remain `waiting` | TEST-025, plus new tests in `join_service_test.rb`, `queue_entries_controller_test.rb`, `configuration_generator_test.rb` |

## P1 requirements (only tracked once P0 is stable, per `scope-and-tradeoffs.md`)

| Requirement | Specification | Implementation | Test |
|---|---|---|---|
| REQ-SHOW-001 (live updates) | OPEN-002, `architecture.md` §7 | — | TEST-018 |
| REQ-SHOW-002 (cache + invalidation) | DEC-013, `api-spec.md` | — | TEST-017 |
| REQ-SHOW-003 (async notification) | `data-model.md` notification_jobs | — | TEST-019 |
| REQ-SHOW-004 (observability) | `01-requirements/non-functional-requirements.md` NFR-OBS-001 | — | manual |
| REQ-SHOW-005 (rate limiting) | — | — | TEST-020 |

## Not tracked here (by design)

Future-scope items (`07-future-evolution/`) have no requirement ID and are intentionally absent from this table — tracking them here would imply they're in scope for implementation.
