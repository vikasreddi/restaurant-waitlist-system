# Agent Decisions

Record of judgment calls the agent made autonomously (within the "may identify contradictions/edge cases/alternatives" governance bound), and of items the agent explicitly declined to decide and surfaced for human review instead. See `ai-development-approach.md` for the governing rules.

## Decisions the agent made autonomously (organizational/editorial, not business rules)

| Item | What the agent chose | Why it's in-bounds |
|---|---|---|
| Project folder location | Asked the candidate directly rather than guessing (`~/Documents/restaurant-waitlist` chosen by the candidate) | Filesystem location is not a product/architecture decision; asking rather than assuming avoided silently placing the repo somewhere the candidate didn't intend |
| Splitting the 7 required diagrams into 7 separate files under `04-diagrams/` (`01-system-context.md` ... `07-architecture-data-flow.md`) rather than one combined file | Editorial/organizational choice | The Phase 3 prompt names 7 diagrams but does not mandate one-file-vs-many; separate files keep each diagram independently reviewable |
| Splitting the domain entity `TableCombination` out as its own row/table in `data-model.md` rather than a field on `Table` | Flagged explicitly as **not decided** — `data-model.md` states both are open until the implementation phase, and only proposes the split as the clearer default for spec-writing purposes | Marked explicitly as non-final in the document itself, not silently committed as a schema decision |
| Requirement/invariant/test ID scheme (`REQ-*`, `INV-*`, `NFR-*`, `TEST-*`, `DEC-*`, `OPEN-*`) | Carried forward and extended consistently from the Phase 1 analysis's own ID scheme | Purely a documentation-traceability convention, not a product decision |

## Session 2 — decisions made under explicit delegated criteria (not silent)

The Session 2 prompt explicitly asked the agent to evaluate two specific items against stated criteria and recommend a choice — this is different from Session 1's items above (organizational/editorial) and different from the still-open items below (genuinely undecided): here the human delegated the *evaluation*, and the agent's output is a recommendation subject to candidate approval, not a unilateral change to an approved decision.

| Item | Agent's recommendation | Criteria the human supplied |
|---|---|---|
| Oversized-group handling (DEC-011) | Option A — reject the join at submission with a validation error | "Prefer the option that gives the clearest correctness guarantee and simplest implementation" (Prompt 3 §7) |
| Technology stack (DEC-012) | Keep the candidate's proposed stack unchanged; no serious issue found | "Do NOT replace it simply because another stack is possible... if the stack is suitable, explicitly recommend keeping it" (Prompt 3 §8) |

Both are recorded in `02-product-decisions/decision-log.md` (DEC-011, DEC-012) with full evaluation and are explicitly labeled as pending the candidate's final sign-off, per the Session 2 prompt's closing line: "the next phase will begin only after the candidate reviews and approves this output."

## Items explicitly NOT decided by the agent — surfaced for human review

These are recorded as open in `02-product-decisions/decision-log.md` (§Open decisions) and must not be treated as resolved by anything else in `documents/`. Resolved this round: `OPEN-001` (DEC-012), `OPEN-003` (DEC-013), `OPEN-004` (partially — key format only), `OPEN-006` (DEC-011).

| ID | Item | Where it surfaces |
|---|---|---|
| OPEN-002 | Live-update mechanism (polling / SSE / WebSockets) | `03-architecture/architecture.md` §7, `04-diagrams/07-architecture-data-flow.md` |
| OPEN-005 | Seating-code format/strength | `05-specifications/api-spec.md` |
| OPEN-007 | Guest abandonment/expiration behavior (no explicit timeout requirement stated in the brief) | `02-product-decisions/decision-log.md`, `07-future-evolution/production-evolution.md` |

None of these three remaining items were addressed by the Session 2 prompt, so none were resolved — they carry forward unchanged.

## Contradictions checked for and not found

**Session 1:**
- Cross-checked the Phase 3 prompt's "Approved Product Decisions" (§3) and "Final Seating Allocation Algorithm" (§4) against the Phase 1-derived requirements (`01-requirements/`) for conflicts — none found; the approved decisions satisfy REQ-QUEUE-001/002/003 as analyzed in Phase 1.
- Checked the starvation policy's "critical rule" (protect the complete configuration, not a single table) against the brief's own worked example — consistent, not contradictory.
- Checked the P0/P1/Future scope split (§10 of Prompt 2) against the brief's "must-have" vs. "show us if you can" vs. "your call" sections — consistent; nothing in P0 as defined here exceeds what the brief calls must-have, and nothing the brief calls must-have was pushed into P1/Future.

**Session 2 (this review):**
- The three genuine defects found in Session 1's own output (starvation-guarantee overstatement, unjustified "Weighted" naming, ambiguous release identifier) are documented in full as CORR-001 through CORR-003 in `ai-corrections.md`, not summarized here.
- Checked the Session 2 prompt's stack-evaluation criteria (§8) against `01-requirements/non-functional-requirements.md` (NFR-CONC-001/002, NFR-IDEM-001/002) for coverage gaps — none found; every hard requirement maps to a concrete PostgreSQL/Rails mechanism (`decision-log.md` DEC-012).
- Checked whether resolving DEC-011 (reject oversized joins) conflicts with any P0 requirement — none found; REQ-GUEST-001 requires accepting *valid* joins, and does not require accepting groups the system has no configuration to seat.

No contradictions were found that required escalation beyond the open items listed above.

## Session 3 (Phase 4 — AI-native configuration) — decisions made under delegated judgment

The Phase 4 prompt explicitly delegated several configuration choices to the agent's judgment rather than specifying them exactly. These are recorded here, not treated as product/architecture decisions (they configure *how agents work*, not *what the product does*), but recorded for the same transparency reason.

| Item | What the agent chose | Reasoning |
|---|---|---|
| MCP servers | **None added.** | The prompt named three candidate categories (git/repo context, doc/spec retrieval, DB inspection) but required determining genuine usefulness first, not adding by default. Git context is already fully served by the `Bash` tool available to every agent (no MCP needed to run `git log`/`git diff`/`git blame`). Documentation/spec retrieval is already fully served by `Read`/`Grep`/`Glob` against a small, flat `documents/` tree — an MCP server would add indirection with no capability gain at this scale. Database inspection is premature: no database exists yet (no Rails app scaffolded), so there is nothing to inspect; this is worth revisiting once Session B (database/domain model) exists, not now. Documented per the prompt's required phrasing in `session-log.md`. |
| Hooks | **None added in `.claude/settings.json`.** | No Rails/npm project exists yet — a lint or test hook would fail immediately (nothing to run), which is worse than no hook. Deferred to the bootstrap session, once `bundle exec rspec`/`rubocop`/a frontend test command actually exist to hook into. |
| Permission-protection mechanism for approved decisions | Used a permission **`ask` rule** (`Edit`/`Write` on `documents/02-product-decisions/**`) instead of a `PreToolUse` hook. | Achieves the same practical goal (force confirmation before an implementation agent edits an approved decision) with a static, easily-verified JSON rule instead of a shell-scripted hook that would need to be constructed and pipe-tested against synthetic stdin per the config skill's own verification workflow — lower complexity and lower fragility risk for a two-day project, per the prompt's explicit "do not create slow/unnecessary hooks" guidance. |
| Agent tool-grants (`spec-reviewer`, `code-review-agent` = read-only; others = read+write+bash) | Restricted `spec-reviewer` to `Read, Grep, Glob` and `code-review-agent` to `Read, Grep, Glob, Bash` (no `Write`/`Edit`) | Enforces "must not implement application code" / "must not silently modify code during review" at the tool-permission level, not only via a prompt instruction an agent could drift from — a stronger guarantee than instruction text alone. |
| Requirement traceability format | A single flat Markdown table (`documents/01-requirements/traceability.md`) covering Requirement → Specification → Implementation → Test, no per-requirement files, no generated tooling | The prompt explicitly said "do not create a complicated tracking system... a simple Markdown mapping or checklist is sufficient" — this is the simplest structure that still lets `code-review-agent` and the `/requirements-check` command do their job. |
| Session-boundary guidance location | Wrote it as a new `documents/06-ai-working-record/session-plan.md` rather than inlining the full Session A–K breakdown into `CLAUDE.md` | Keeps `CLAUDE.md` itself short (it links to session-plan.md); the source prompt's own instruction was "do not duplicate large sections of these documents into configuration files." |

None of these are product/architecture decisions and none required a `BLOCKED — HUMAN DECISION REQUIRED` escalation — they were explicitly left to agent judgment by the governing prompt itself ("use your judgment," "if no MCP provides meaningful benefit... this is an acceptable outcome," "if hooks are not necessary, document why").

## Contradictions checked for — Session 3

- Checked the four agents' assigned tool grants against `CLAUDE.md`'s AI rules ("must not... claim completion without verification") — `backend-domain-agent` and `test-engineering-agent` both have `Bash` (needed to actually run tests), so "claim completion without verification" is a prompt-level rule for those two, not a tool-level one; no contradiction, just a different enforcement layer than the read-only agents.
- Checked that no agent, skill, or command in `.claude/` implements, references, or scaffolds a `documents/07-future-evolution/` item — none do.
- Checked that the technology stack referenced throughout `.claude/` matches the approved DEC-012 stack exactly (no drift, e.g., no accidental mention of a different queue/cache technology) — confirmed consistent.

No contradictions found requiring escalation.

## Session 4 (Phase 5A — bootstrap) — environment decision surfaced to the candidate, not decided silently

| Item | What happened | Why it was surfaced rather than decided unilaterally |
|---|---|---|
| Container runtime choice (no Docker/Colima/Podman found on the machine) | Agent presented four concrete options (Colima+CLI, Docker Desktop, candidate installs manually, skip Docker/proceed unverified) via a direct question rather than picking one. **Candidate chose Colima + Docker CLI.** | Installing a container runtime is a meaningfully impactful, not-trivially-reversible system change (background VM, networking changes, potential GUI/privileged-helper setup) — Prompt 5 §2 itself explicitly forbids installing global software automatically, and this falls squarely in that category even though Docker is required infrastructure for the approved stack (DEC-012). |
| Response to the Homebrew `/usr/local` permission failure | Agent did not attempt `sudo chown` itself, and did not attempt any workaround (e.g., reinstalling Homebrew to a user-writable prefix, using `--build-from-source`, or a manual tarball install of Colima/Docker binaries bypassing Homebrew). Reported the exact fix and asked the candidate to run it. | The fix requires `sudo` (the candidate's password, which the agent cannot supply) and touches ~25 system-wide directories under `/usr/local` that affect every Homebrew-managed tool on the machine, not just this project — well outside "project-local" scope, and irreversible-by-the-agent if done wrong. Matches the standing guidance to check before hard-to-reverse, broad-blast-radius actions. |

This is recorded as a **process/environment decision**, not a product or architecture decision — it does not touch anything in `documents/02-product-decisions/` and required no `BLOCKED — HUMAN DECISION REQUIRED` escalation (that format is reserved for architectural contradictions, missing product decisions, or correctness-affecting ambiguity, none of which occurred here — this was a pure tooling/permissions blocker).

## Session 5 (Phase 5A resume — bootstrap completed) — decisions made autonomously and ones surfaced

| Item | What happened | In-bounds reasoning |
|---|---|---|
| Manually symlinking `docker`, `colima`, `limactl` into `/usr/local/bin` after `brew link` failed only on an unrelated fish-completions directory | Agent did this itself, without asking, rather than requesting a third `sudo chown` round | Judged as the "smallest necessary correction" under the troubleshooting rule (Prompt 7 §10): the formulae were already installed by Homebrew into `/usr/local/Cellar` (the part that succeeded); the only failure was linking convenience files for a shell (fish) nobody here uses. Creating symlinks in an already-writable directory (`/usr/local/bin`, confirmed group-writable) to already-installed, Homebrew-placed binaries is non-destructive, fully reversible (`brew unlink`/`brew link` still work normally), and installs nothing new — a meaningfully different, much lower-stakes action than the sudo-requiring chown calls that were escalated. |
| Surfacing the Rosetta/Homebrew-architecture discovery to the candidate rather than silently installing a native Homebrew | Agent stopped and asked, with concrete options, rather than picking one | This was a genuinely new architectural fact about the machine (not something either prior session had reason to know), and the fix (a second Homebrew installation, a meaningful and slightly unusual system change) itself needed an interactive sudo password the agent cannot supply — squarely the same "stop and ask for a human administrator action" case as Session 4's permission issues, per Prompt 7 §2 and §10. |
| Rails version: pinned `~> 7.1.6` (not Rails 8) | Agent's implementation-phase choice, not escalated | `documents/03-architecture/architecture.md` OPEN items never specified an exact Rails minor/major version — only "Ruby on Rails API" was approved (DEC-012). Rails 7.1 was chosen specifically to avoid Rails 8's newer default scaffolding (Kamal deploy config, Solid Queue/Cache/Cable) that would otherwise need explicit `--skip-*` flags to stay consistent with "do not introduce unnecessary infrastructure" — a judgment call within the approved stack, not a deviation from it. |
| Ruby 3.3.12, Node 20, minitest pinned to `~> 5.20` | Agent's implementation-phase choices | Ordinary dependency/runtime version selection within an already-approved stack; the minitest pin specifically fixes a real compatibility bug (see `session-log.md` Session 5 "Problem encountered"), not a design choice. |
| `--skip-git` on `rails new` | Agent's choice, not asked | The overall project has no git repository (true since Session 1, never established). Letting `rails new`'s default `git init` create a nested repository inside `backend/` only would have been a worse, harder-to-notice state than no repository at all. Not initializing the top-level repository either was already the standing position from Session 4 — carried forward, not re-decided. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the symlink and version-pin items are ordinary implementation judgment calls within already-approved scope (per `CLAUDE.md` "AI rules: agents may... implement within assigned scope"), and the two environment items that genuinely warranted a stop (residual permissions, Rosetta architecture) were in fact stopped and surfaced, not decided silently.

## Contradictions checked for — Session 5

- Checked that nothing generated by `rails new` or `create-vite` (default templates, boilerplate files, generated READMEs) implements or references any P0 business behavior — confirmed clean; only the explicitly-added `HealthController` and status-page `App.tsx` touch application logic, both within Prompt 5/7's explicit bootstrap scope.
- Checked the `rack-cors` configuration against `documents/02-product-decisions/decision-log.md` DEC-013 (Redis/cache boundary) — not applicable/no conflict; CORS is a browser-security concern unrelated to the cache-authority invariant.
- Checked that `backend/bin/docker-entrypoint`'s `rails db:prepare` call does not implicitly create any business schema — confirmed; zero migrations exist, so it only creates Rails' own internal tracking tables (`schema_migrations`, `ar_internal_metadata`), consistent with Prompt 5/7 §7 ("no business schema yet, only whatever minimal Rails database setup proves the app can connect").

No contradictions found requiring escalation.

## Session 6 (Phase 5A.1 — cleanup + git checkpoint) — judgment calls

| Item | What happened | Reasoning |
|---|---|---|
| Default git branch named `main` (not git's own default `master`) | Agent set this via `git symbolic-ref HEAD refs/heads/main` before the first commit, without asking | Ordinary, low-stakes tooling convention (now the standard default on GitHub/GitLab and most modern tooling); not a product/architecture decision, fully within "agents may... implement within assigned scope" for a task explicitly about setting up git. |
| `.gitignore` includes `backend/config/master.key` even though the governing prompt's example list didn't name it | Agent added it | The prompt's §7 explicitly says "do not commit secrets, passwords, private credentials" — Rails' `config/master.key` is exactly that category (it's the decryption key for `credentials.yml.enc`); omitting it would have been a real gap in "inspect before commit," not a faithful reading of the instruction's intent. |
| Generating `frontend/package-lock.json` (missing from the host) before committing | Agent did this as part of §7's "review the files that will be committed," not asked for explicitly | Discovered during the required pre-commit review, not invented busywork; committing a Node project without its lockfile would undermine "the commit should represent the Phase 5A runnable baseline" (§7/§8) — a reproducibility gap directly relevant to what this checkpoint is for. Did not touch anything else in `frontend/` or `backend/` beyond this. |
| Author identity on the first commit (`vikas@Rajas-Air.lan`, auto-derived) | Agent did not configure `git config user.name`/`user.email` globally or locally, and did not guess the candidate's real name/email | Guessing or fabricating a person's name/email for commit authorship isn't the agent's call to make; flagged to the candidate in the session report instead, so they can set it (and optionally amend the commit) themselves if it matters for how the assignment reads. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — all four are ordinary tooling/process judgment calls within the explicit scope of "git setup" and "inspect before commit," not product, architecture, or business-behavior decisions.

## Session 7 (Phase 5B.1 — domain model proposal) — assumption changes and design decisions

These are **specification decisions**, made under this phase's explicit mandate to design the domain model (not implement it) — recorded here per the governing prompt's own instruction ("if the agent identifies an ambiguity or changes a previous assumption, record it"), and distinguished from the "Session 5B.1 open decisions" list inside the deliverable itself (`05-specifications/domain-model-proposal.md` §16), which is reserved for things a human still needs to decide.

| Item | What changed | Why it's a reasoned revision, not a silent one |
|---|---|---|
| Added a `ready` `QueueEntry` state (Phase 3 draft had none) | Staff "seat by code" now confirms an already-reserved `SeatingAssignment`, rather than performing allocation synchronously | Traced directly to the brief's own wording ("when a group reaches the front, their phone shows a code") — the synchronous version in Phase 3's `functional-spec.md` §6 couldn't actually produce that guest experience. Flagged explicitly in the deliverable's §0, not applied to `domain-model.md`/`functional-spec.md` directly. |
| Replaced `TableCombination` with `SeatingAssignment` + `SeatingAssignmentTable` | Combination is now a property of an assignment (1 or 2 member-table rows) rather than its own top-level entity | Chosen specifically so a single database constraint (partial unique index on the join table's `table_id`) can correctly enforce table exclusivity regardless of which "slot" (table-one/table-two) a table occupies — a correctness property the simpler two-FK-column alternative could not provide (full comparison in the deliverable §14). |
| Dropped `IdempotencyRecord`, `GuestIdentity`/`ActiveVisitToken`, and `NotificationJob` as separate entities from the Phase 3 draft | Idempotency key and guest token both became plain unique columns on `QueueEntry`; notifications dropped entirely from this phase | Each was checked against what the requirement actually needs (idempotency: "does a row for this key exist" — one column answers it; guest identity: explicitly scoped to "the active queue entry," not a guest-spanning concept; notifications: explicitly P1/deferred) — three fewer entities than the Phase 3 sketch, a direct application of "do not invent requirements that are not necessary." |
| No stored "position" or "starvation weight"/"protected since" column | Both fully derived at read time | This phase's own §7/§8 explicitly asked the agent to prove necessity before persisting a precomputed value — no proof existed (both are cheaply derivable from `joined_at`/`group_size`/current table state), so neither was added. |
| Did not update `03-architecture/domain-model.md` / `data-model.md` to match this proposal | Deliberate — recommended in the deliverable, not performed | These are treated as "approved" documents per `CLAUDE.md`'s source-of-truth framing; revising them silently would be exactly the "silently changing a product decision" the AI governance rules prohibit, even though the change here is a specification refinement, not a business-rule change. Left for explicit human sign-off. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — they are the actual work product this phase asked for (a domain model design, with alternatives compared and justified), not architectural contradictions or business-rule changes. The one thing that *was* escalated rather than decided is listed in the deliverable's own §16 (Open Decisions): the `ready`-state abandonment-timeout question, and the still-unresolved `OPEN-005` seating-code format.

## Contradictions checked for — Session 7

- Checked whether the new `ready` state conflicts with `INV-009` ("a QueueEntry can be seated at most once") — no conflict; `ready → seated` is still a single, one-way, non-repeatable transition per entry, `ready` just adds a documented intermediate step before it.
- Checked whether dropping `IdempotencyRecord` as a table loses any capability the Phase 3 draft relied on elsewhere (e.g., `04-diagrams/06-guest-join-idempotency.md`) — reviewed that diagram; it already described the mechanism at the "unique constraint" level, not the "separate table" level, so no other document assumes a dedicated `IdempotencyRecord` table exists.
- Checked the proposed `SeatingAssignmentTable` design against `INV-005`/`INV-008` (atomicity, no conflicting assignments) — confirmed the partial unique index plus single-transaction insert satisfies both, per the concurrency plan (deliverable §11).

No contradictions found requiring escalation beyond what's already listed in the deliverable's §16.

## Session 8 (human review of Phase 5B.1) — asked rather than assumed

| Item | What happened | Why |
|---|---|---|
| Expiration policy specifics (duration; what happens to the entry/table on expiry: auto-`no_show`, back to `waiting`, or something else) | Candidate stated an expiration policy is being introduced to prevent `ready` reservations holding tables indefinitely, but didn't specify the shape. Agent asked clarifying questions rather than picking a default and encoding it into the model. | This is a genuine product/fairness-policy decision of the same weight as `DEC-004` (the original starvation policy) — not an implementation detail the agent should fill in unilaterally. It also directly interacts with a decision the proposal itself already made and explained (`ready → waiting` is "not modeled in this MVP," `domain-model-proposal.md` §6) — if the expiration behavior is "back to waiting," that decision needs to be revisited explicitly, not silently reversed by picking an answer without asking. |

No `BLOCKED — HUMAN DECISION REQUIRED` needed — the candidate had already opened this as a decision in progress; the agent's role here was to gather the specifics needed to encode it correctly, not to identify a new blocker.

## Session 9 (Phase 5B.1 finalization) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Adopted the candidate's `released_at`-based constraint design over the agent's own Session 8 fix | The candidate proposed `UNIQUE(table_id) WHERE released_at IS NULL` and asked the agent to validate it against six specific cases (pending, active, release, expiration, historical assignments, atomic two-table seating). Agent validated all six explicitly and adopted the design as strictly better than its own prior fix. | The Session 8 fix (delete rows on release) was technically valid but lost historical seating data — a real, if secondary, regression the candidate's design avoids. Recording *why* the second fix is better than the first, not just that it replaced it, keeps the audit trail honest rather than implying the first fix was already perfect. |
| Updated `03-architecture/domain-model.md` and `data-model.md` directly, in place | Session 7 had explicitly deferred this ("recommend... not performed automatically") pending human sign-off. This session's governing message explicitly said to do it ("also update relevant architecture/data-model documentation where stale Phase 3 assumptions conflict with the finalized model"). | The prior deferral was itself a recorded decision (`agent-decisions.md` Session 7) waiting on exactly this kind of explicit authorization — not overridden silently, but resolved by the human input it was waiting for. |
| Did **not** extend the same update to `05-specifications/functional-spec.md`, `allocation-spec.md`, `api-spec.md`, `test-strategy.md`, or the four affected diagrams | Agent flagged these as a known, unaddressed inconsistency instead of silently leaving them stale *or* unilaterally rewriting ~7 more files beyond what was asked. | The governing message's instruction was specifically scoped to "architecture/data-model documentation" — a narrower category than "all specifications." Expanding scope without being asked (even to fix a real inconsistency) risks exactly the kind of unrequested, hard-to-review sprawl `CLAUDE.md`'s engineering rules warn against ("prefer small changes," "do not silently expand scope"). Flagging it clearly, in both `session-log.md` and this file, is the middle path between silent inconsistency and silent scope creep. |
| Added `INV-016`/`INV-017` to the invariant list rather than rewording an existing one | New invariants, not edits to `INV-001`–`INV-015`'s meaning | Both are genuinely new rules (the self-contained-constraint discipline; the lazy-expiration behavior) that didn't exist in any form before this session — appending preserves the existing invariants' stable meaning/numbering for anything that already cites them (e.g., `CLAUDE.md`, prior session logs). |

## Contradictions checked for — Session 9

- Checked the finalized `released_at` design against every one of the six cases the candidate explicitly asked for (pending/active/release/expiration/historical/atomic two-table) — all six pass, documented in `domain-model-proposal.md` §2.
- Checked whether DEC-015's "no background sweep" requirement conflicts with anything already committed to Redis/Sidekiq being P1-only (DEC-013 boundary) — no conflict; lazy evaluation needs no cache or job infrastructure at all, so it doesn't even touch that boundary.
- Checked whether the new `INV-016`/`INV-017` numbering collides with anything else that might reference "the last invariant" implicitly — found and fixed one stale reference (`CLAUDE.md`'s "INV-001–INV-015" range).

No contradictions found requiring escalation.

## Session 10 (Phase 5B.1.5 — consistency pass) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Audited every document before editing any of them, and presented the full contradiction table before proceeding | Followed the governing prompt's explicit §2 instruction literally, rather than fixing documents opportunistically while reading them | A partial, edit-as-you-go pass risks missing cross-document inconsistencies (e.g., fixing `functional-spec.md`'s seat-by-code flow without noticing `test-strategy.md` still assumed the old flow) — a complete audit first, with the table as a checkpoint, is what actually delivers "consistency," not just "some documents improved." |
| Extended `.claude/agents/` and `.claude/skills/` fixes beyond what the governing prompt's document list named | The prompt's audit scope was `documents/01-requirements/` through `documents/06-ai-working-record/` — it didn't mention `.claude/`. The agent found stale `INV-001–INV-015` references there anyway (during the general invariant-numbering check) and fixed them. | These files are read by every future implementation session exactly like `CLAUDE.md` is — leaving them stale while fixing the equivalent reference in `CLAUDE.md` itself would have been an inconsistent, arbitrary boundary, not a faithful reading of "bring specifications into consistency." Scope was extended by one clear, bounded degree (three files, one specific stale fact), not broadly. |
| Classified every remaining historical reference as "correctly preserved" rather than silently leaving the classification implicit | The governing prompt's §12 asked for exactly this three-way classification (historical / future-deferred / actual contradiction) to be *done*, and its §13 final report asked for "documents intentionally unchanged" with reasons | Doing the classification but not stating it anywhere would leave a future reviewer unable to tell "not touched because it's fine" from "not touched because it was missed" — the whole point of an audit-first pass is that it's checkable. |
| Did not touch `documents/07-future-evolution/` despite it containing forward-looking domain concepts (fairness debt, shared tables) that reference now-superseded entity names in places | Judged out of scope | Those documents describe features explicitly *not* being built — their entity sketches are illustrative of a hypothetical future extension, not live specification the finalized model needs to match. Rewriting them to track every schema revision of a feature that doesn't exist yet would be scope creep with no reader benefit. |

No `BLOCKED — HUMAN DECISION REQUIRED` needed this session — no requirement was found to genuinely conflict with the finalized model (checked explicitly per governing prompt §9), and no new product/architecture decision was required, only propagation of decisions already made in Sessions 8–9.

## Contradictions checked for — Session 10

- Ran the full audit named in the governing prompt's §2 (READY lifecycle, staff seat-by-code, table representation, idempotency, guest identity, table exclusivity, READY expiration) against every document in scope — findings are the 21-item contradiction table delivered in the session's chat response, not duplicated here.
- Checked each of the 15 must-have requirements the prompt named explicitly (REQ-GUEST-001–005, REQ-STAFF-004/005/006, REQ-TABLE-002/005, REQ-QUEUE-001/002/003, REQ-INFRA-001/002) against the updated specifications — all still map cleanly; no requirement content was changed to fit the model, only wording tightened.
- Re-ran the stale-reference grep (`TableCombination`, `IdempotencyRecord`, `GuestIdentity`, `idempotency_records`, `Table.status`, direct `waiting → seated`) after all edits, to confirm the fixes were actually complete and not just applied to the files explicitly named in the prompt.

No contradictions found requiring escalation beyond what's already itemized in the session's contradiction table and final report.

## Session 11 (Phase 5B.2 — domain persistence implementation) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Added stored `expires_at` on `seating_assignments` | Set at creation from a new `SeatingAssignment::READY_TIMEOUT` constant (ENV-configurable, default 300s) | The governing prompt's §11 explicitly listed `expires_at` as a field to implement, in some tension with `domain-model-proposal.md` §8's earlier "derive, don't store" reasoning for the same threshold. Judged as a pure implementation detail under this prompt's own §0 delegation, not a business-rule change — storing a frozen deadline for one specific reservation doesn't alter what DEC-015 actually guarantees, and the prompt's explicit field list is a reasonable instruction to follow rather than override on the strength of an earlier, more abstract design note. |
| `TableAdjacency` canonical-pair storage (`table_id < adjacent_table_id` check constraint) | Chosen over "store both directions" or "dedupe in application code" | Directly answers the prompt's own §6 concern ("ensure reverse duplicates cannot represent a second logical relationship") with a single database constraint rather than write-path logic that could be bypassed by a future direct-SQL writer. Also incidentally rejects self-adjacency for free (`table_id == adjacent_table_id` can never satisfy `<`), which the prompt listed as a separate requirement (§23) — one constraint doing double duty was preferred over two, per "prefer a canonical pair representation if it is simpler" (§6). |
| `seating_code` kept on `queue_entries`, not duplicated onto `seating_assignments` | Resolved a wording ambiguity between this prompt's §11 (loosely listed it among `SeatingAssignment`'s fields) and the already-finalized `data-model.md` (explicitly puts it on `queue_entries`) | This prompt's own §1 names `data-model.md` as one of the primary sources of truth to read first — deferred to the more specific, already-approved document rather than a looser phrase in the new prompt. Documented explicitly (not silently) in both `data-model.md`'s new addendum and here, so the ambiguity and its resolution are both visible, not just the outcome. |
| Two Rails-level validations added beyond what any DB constraint requires (`SeatingAssignmentTable#table_not_already_claimed`, `#assignment_has_at_most_two_tables`) | Agent's own addition, not explicitly requested per-field | The prompt's §26 explicitly asks for Rails validations "for developer/user-facing validation where appropriate" while keeping DB constraints authoritative for concurrency-sensitive cases — these two give a readable `ActiveRecord::RecordInvalid` message in the common single-request path instead of always surfacing a raw `PG::UniqueViolation`, without being relied on as the actual safety mechanism (each has an explicit code comment saying so, and each has a corresponding "enforced at the database level" test that bypasses the validation to prove the DB constraint alone still holds). |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — all four are implementation-level judgment calls explicitly delegated by the governing prompt's own §0 ("you may make normal implementation-level decisions... choose the simplest correct implementation and document it"), not business-rule changes, scope expansion, or replacement of an approved domain decision.

## Contradictions checked for — Session 11

- Checked whether any of the three field/design decisions above touch a `DEC-*` decision or `INV-*` invariant — none do; all three are schema-shape or config-value choices within already-approved boundaries.
- Checked the actual generated PostgreSQL schema (not just the migration source) against every item in the governing prompt's §34 checklist — confirmed via direct `psql \d` inspection, not assumed.
- Checked that no test relies solely on a Rails-level validation to prove a concurrency-sensitive invariant — every "enforced at the database level" test explicitly bypasses Rails validations (via `update_column`, raw `connection.execute`, or `save!(validate: false)`) to prove the underlying constraint holds independent of application code, per this project's own `hard-path-testing` skill and CLAUDE.md's engineering rules.

No contradictions found requiring escalation.

## Session 12 (Phase 5B.3 — guest join API + idempotency) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Route stayed `/guest/queue-entries`, not the prompt's illustrative `/guest/join` | Followed the governing prompt's own §4 instruction to not invent a different URL structure when `api-spec.md` already defines one | `api-spec.md` had already specified `POST /guest/queue-entries` since Phase 5B.1.5's consistency pass. The prompt's `/guest/join` was explicitly labeled a "conceptual operation," not a mandate, and its own text says to follow the approved spec's route when one exists. |
| Request field stayed `phone_number`, not the prompt's illustrative `phone` | Same reasoning as above, per the prompt's §5 ("exact parameter names must follow api-spec.md if they differ") | `api-spec.md`'s join request shape (`{ group_size, phone_number, idempotency_key }`) predates this prompt and was left unchanged; the prompt's own JSON example used a shorter field name but explicitly deferred to the spec. |
| `position` omitted entirely from the 201/200 response body | Judged as required by the prompt's own §18 ("do NOT implement queue position... do not prematurely implement this in the join endpoint"), even though `api-spec.md`'s pre-existing response shape listed `position` | This is a real, if narrow, tension between two approved documents (the prompt's explicit prohibition vs. the spec's response shape) rather than a free implementation choice — resolved in favor of the newer, more specific instruction (the prompt governing *this* phase) over the older, more general spec text, and made non-silent by adding an explicit "Implementation status (Phase 5B.3)" note to `api-spec.md` itself explaining the omission and what would restore it (the allocation/position service, a later phase). This was judged NOT to rise to `BLOCKED — HUMAN DECISION REQUIRED`, since the prompt itself directly and explicitly resolves the tension (§18 is unambiguous) — it isn't a case of the agent inventing an answer where none was given. |
| DEC-011 (oversized-group rejection) also deferred, alongside `position` | Not explicitly named in the prompt, but judged to follow from the same §17/§18 no-allocation boundary | Evaluating "does any configuration exist that can seat this group" requires knowing the set of seatable table/adjacency configurations — the same allocation-adjacent knowledge `position` requires and that §17 explicitly forbids this phase from touching (table availability inspection, table/adjacency selection). Documented as a deliberate divergence in `api-spec.md`, not a silent gap — `validation_error` in this phase only covers `group_size <= 0` and blank `phone_number`. |
| Removed `uniqueness: true` from `QueueEntry#idempotency_key` (CORR-005) | See `ai-corrections.md` CORR-005 for the full bug account | A genuine concurrency-correctness bug self-caught by the project's own concurrency test, not a judgment call — recorded as a correction, not a decision, but cross-referenced here since it changed a Phase 5B.2 model file during this phase. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` in the end — the route/field-name items were direct applications of the prompt's own explicit "defer to the existing spec" instruction, and the `position`/DEC-011 omissions were direct applications of the prompt's own explicit no-allocation boundary (§17/§18), not cases of the agent inventing a resolution to a genuinely open question.

## Contradictions checked for — Session 12

- Checked `api-spec.md`'s pre-existing join response shape (which listed `position`) against this prompt's explicit §18 prohibition on implementing position in this phase — a real but narrow tension, resolved as above and made visible in the spec itself rather than silently reconciled.
- Checked whether `group_size`/`phone_number` validation as implemented (presence/positivity only) still satisfies every requirement this phase claims — confirmed DEC-011's fuller oversized-group check is out of reach without allocation logic, and documented it as deferred rather than silently narrowing the requirement's claimed scope in `traceability.md`.
- Checked that the CORR-005 fix (removing a Rails-level uniqueness validation) doesn't weaken any invariant — confirmed the database's unique index on `idempotency_key` (added in Phase 5B.2, unchanged) remains fully authoritative; only the redundant, racy application-level check was removed, per CLAUDE.md's own engineering rule against a correctness guarantee depending on two mechanisms that can diverge.

No contradictions found requiring escalation beyond the `position`/DEC-011 divergence already itemized and explained above and in `api-spec.md`.

## Session 13 (Phase 5B.4 — guest current queue status + position) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Position implemented as a chronological rank among currently-`waiting` entries only | Resolved the prompt's own §3 "reconcile position semantics" task | `functional-spec.md` §9/DEC-005 define the eventual position as compatibility+availability+aging+starvation-aware; this phase's own §4/§10 explicitly forbid implementing any of those factors until Phase 5B.5. With every allocation-aware input off-limits, chronological join order among `waiting` entries is the only deterministic signal left — chosen not because it's "the real position" (it explicitly isn't) but because it's the simplest representation the current phase's own boundary permits, and it was made non-silent via a dedicated "Position semantics" note in `api-spec.md` rather than left implicit. Judged NOT to require `BLOCKED — HUMAN DECISION REQUIRED`: the prompt's §4 directly authorizes exactly this ("the position may initially be derived from the currently waiting queue using the simplest deterministic representation... If the existing requirements explicitly require a different position semantic, follow the requirement and document the distinction") — the tension is real but the prompt itself resolves it, so this isn't a case of inventing an answer to an open question. |
| Token transport fixed as `Authorization: Bearer <active_visit_token>` | `api-spec.md` had explicitly left this "an implementation-phase detail, not yet fixed" — this is the first phase that actually reads a guest token, so the choice had to be made now | A header keeps the token out of server/proxy access logs and browser history, unlike a query string or path segment — directly serves this phase's own §16 token-security instruction ("avoid exposing it in error messages," extended here to avoid exposing it in access logs too). `Bearer` is the conventional scheme for an opaque bearer credential and needed no new library or session mechanism. A pure implementation-level decision under §1's delegation, not a product-behavior change — documented in `api-spec.md`'s Conventions section so it's now fixed for future phases (e.g., a future `POST /guest/queue-entries/current/leave`) to reuse rather than re-decide. |
| Invalid/missing token both return literal `404`, not `401` or a `200` with a "no active visit" flag | `api-spec.md` already listed `404 / equivalent "no active visit"` as the target behavior but didn't pin the exact status code; the prompt's §14/§15 explicitly allow picking among documented options | `api-spec.md`'s own phrasing groups "missing" and "unknown" token into one outcome, described as "treated as 'start a new join,' not a server error" — a literal `404 Not Found` on the resource ("no current visit exists for you") reads most naturally as that outcome, is a standard REST status for "nothing here," and keeps a single code path for every reason the token doesn't resolve (matching functional-spec.md §2's "token unknown/invalid → guest is treated as not having an active visit," which doesn't distinguish sub-cases either). Documented in `api-spec.md` so this stays fixed going forward rather than re-decided per phase. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the position-semantics item is a direct application of the prompt's own explicit §4 authorization, and the transport/status-code items are ordinary implementation-level decisions (§1) filling in details `api-spec.md` had deliberately left open, not changes to any approved product decision.

## Contradictions checked for — Session 13

- Checked `functional-spec.md` §9/DEC-005's full position definition against this phase's explicit no-allocation boundary (§4/§10) — the real, narrow tension addressed above, resolved per the prompt's own explicit authorization and documented in `api-spec.md`, not silently reconciled.
- Checked whether the chronological-rank position could be mistaken for (or later silently promoted to) the final allocation priority — added an explicit non-guarantee statement to `api-spec.md`'s "Position semantics" note and a corresponding test category (`current_queue_status_service_test.rb`'s "a non-waiting entry does not count toward another entry's position" and the position-ordering test) so the simplification's actual behavior is pinned down, not just described.
- Checked that the DEC-015 lazy-expiration logic added this session doesn't duplicate or diverge from the concurrency pattern `domain-model-proposal.md` §11 already specified for "two concurrent operations both notice the same overdue ready reservation" — confirmed the same `SELECT ... FOR UPDATE` row-lock approach was used, not a new mechanism.
- Confirmed no FIFO framing was proposed anywhere as the *final* seating priority (the prompt's own explicit trip-wire, §25) — the chronological position is repeatedly and explicitly labeled informational/non-final in both the code comments and `api-spec.md`, so no correction was needed here.

No contradictions found requiring escalation beyond the position-semantics divergence already itemized and explained above and in `api-spec.md`.

## Session 14 (Phase 5B.5.1 — allocation algorithm reconciliation) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| `fit_score` defined as a utilization ratio (`group_size / configuration.capacity`), not a raw seat-waste difference | Agent's own formula design, under this phase's explicit "define actual formulas, avoid vague language" instruction (§27) | A ratio is naturally bounded `(0,1]` and directly expresses the actual business goal (capacity utilization) in a way comparable across differently-sized configurations — wasting 2 seats out of 4 is a materially worse decision than wasting 2 seats out of 8, which a raw-difference formula would score identically. |
| `scarcity_score` defined per-entry as `1 / count(currently-compatible available configurations)`, recomputed fresh every pass | Agent's own formula design, directly required by §7's "based on the currently available configuration/capacity landscape," explicitly not a static seed-time assumption | Answers "how many options does this specific waiting group have right now" without any predictive/historical component — satisfies §7's explicit ban on a "full predictive optimization system" while still capturing real opportunity cost (a group with one option is more urgent than a group with several). |
| `aging_score` capped at `1.0`, with `MAX_AGING_WINDOW_SECONDS` defaulted equal to `STARVATION_THRESHOLD_SECONDS` (1200s) | Agent's own parameter choice, within this phase's explicit "tunable MVP parameter" labeling requirement (§27) | Chosen so ordinary aging saturates to its ceiling at exactly the moment the separate, categorical starvation-protection mechanism engages — avoids two different mechanisms both trying to express "very long wait" past that point; documented as the resolution to §10's "maximum-weight safeguard" requirement (capping the one genuinely unbounded-growth-prone term is sufficient, since `fit_score`/`scarcity_score` are already naturally bounded). |
| Default weights `WEIGHT_FIT=0.4, WEIGHT_SCARCITY=0.3, WEIGHT_AGING=0.3` | Agent's own parameter choice, explicitly labeled "tunable MVP parameter" per §27's requirement, not asserted as a product truth | `fit` weighted highest because it's the only one of the three terms that differentiates between a single group's own multiple compatible options (preserving `seating-allocation-policy.md` Stage 3's role); `scarcity` and `aging` weighted equally because both represent a distinct "protect the group with fewer other paths to being seated" signal (alternative-count vs. elapsed-time) with nothing in the approved requirements arguing either should dominate the other. |
| Global batch-matching loop (`run_allocation_pass`: build the full grid, pick the single best candidate, allocate, repeat until exhausted) rather than a single global optimization solve | Agent's own algorithm-shape decision, directly required by §14's "consider all currently eligible candidates together... choose the deterministic highest-priority candidate," explicitly not first-match | A repeat-until-exhausted greedy loop is simple, deterministic, and cheap at MVP scale (§28 explicitly forbids optimization solvers) while still satisfying "global," since every iteration re-evaluates the complete current grid rather than committing to a stale or partial view. |
| Tie-break hierarchy: `total_score` → fewer tables consumed → earlier `joined_at` → lower `id` — **not** the 6-level hierarchy the prompt itself suggested as an example | Deliberately deviated from the prompt's own suggested ordering, per its explicit §21 instruction not to auto-accept it | The suggested hierarchy's "better compatibility/lower waste" and "overall priority score" levels would double-count the same signal `fit_score` already contributes inside `total_score` — collapsing them avoids redundancy. Only "fewer tables consumed" survives as its own tier, because it captures a genuine residual signal `total_score` cannot already express (an exact `fit_score` tie between a single-table and a combined-table candidate for the same group). |
| Starvation-protected-candidates ordered by the same `total_score`+tie-break chain, not `allocation-spec.md`'s existing `oldest_by_wait_time(protected)` shortcut | A deliberate refinement over the pre-existing pseudocode, flagged explicitly as filling a genuine gap in the approved policy | `starvation-policy.md`/`seating-allocation-policy.md` only guarantee a protected group's priority *over non-protected competitors* — neither document specifies how to order two or more *simultaneously* protected groups against each other. Since every protected entry already has `aging_score` capped at `1.0` (no longer differentiating), reusing the same composite score lets `fit`/`scarcity` still meaningfully differentiate among protected candidates, falling back to `joined_at` only on a further tie — richer than "oldest always wins" without weakening the one guarantee that *is* approved (a protected candidate always beats a non-protected one). Flagged in the new document's "Known limitations" section as the agent's own reasonable completion of an open gap, not itself an approved product decision, so a human reviewer can override it before Phase 5B.5.2. |
| `allocation-spec.md` §4 corrected to remove a stored `starvation_protected_since` field (see `ai-corrections.md` CORR-006) | See CORR-006 for the full account | A genuine, real specification contradiction self-caught by this phase's own required cross-document comparison, not a judgment call — recorded as a correction, cross-referenced here since it changed a Phase 5B.1-era document during this phase. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — all are either direct applications of this phase's own explicit instructions (define formulas, label tunable parameters, don't auto-accept the suggested tie-break order, compare against every document and correct genuine contradictions) or ordinary implementation-level judgment calls within the algorithm-design latitude this phase's prompt explicitly granted ("AI may help us derive or validate the algorithm"). The one deliberately-flagged open gap (starvation-protected ordering) was resolved with a documented, reasonable default and explicitly surfaced for human review rather than silently asserted as settled.

## Contradictions checked for — Session 14

- Ran the explicit cross-document comparison this phase's own §23 required against `seating-allocation-policy.md`, `starvation-policy.md`, `functional-spec.md`, `api-spec.md`, `domain-model.md`, `data-model.md`, `domain-model-proposal.md`, and `07-future-evolution/missed-opportunities.md` — found and corrected the one genuine contradiction (CORR-006); everything else was consistent, only needing formulas added where prior documents had left concepts qualitative.
- Ran the full §30 verification checklist (12 items: FIFO grep, position-vs-priority distinction, 1–2 table maximum, 6-seat standalone, 2+2/4+4 adjacency, configurable threshold, READY semantics, allocation≠confirmation, future-fairness separation, AI-runtime-exclusion) — all confirmed, documented in `session-log.md`.
- Confirmed via `git status` that zero files under `backend/` were touched this session, satisfying the phase's strict "no application code" boundary before proceeding to any documentation edits.

No contradictions found requiring escalation beyond CORR-006, already corrected and recorded.

## Session 15 (Phase 5B.5.2 — pure allocation decision engine) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| `Allocation::Policy`'s threshold constants stored as plain `Integer` seconds, not `ActiveSupport::Duration` (unlike `SeatingAssignment::READY_TIMEOUT`) | Deliberate small deviation from the exact idiom the prompt pointed to as precedent | `READY_TIMEOUT` is added to a `Time` to produce a deadline — a genuine `Duration` use case. `STARVATION_THRESHOLD_SECONDS`/`MAX_AGING_WINDOW_SECONDS` are instead divided against a raw elapsed-seconds `Float` inside `aging_score` — a plain `Integer` is the more direct, unsurprising fit for that arithmetic, and the "_SECONDS" naming (carried over verbatim from the locked specification and this phase's own prompt) already signals a plain numeric, not a `Duration` object. |
| `DecisionEngine` defensively filters its own `waiting_entries` input to `status == "waiting"`, rather than trusting the caller to have already scoped the query | Direct response to this phase's own explicit property-test requirement ("non-WAITING entries are ignored," §26) | The prompt's §5 could be read either as "the caller must only pass waiting entries" or "the engine must ignore non-waiting entries if any slip through" — the explicit property-test requirement resolves the ambiguity in favor of the engine defending itself, which is also strictly safer (a future caller passing an unfiltered `QueueEntry.all` can never accidentally leak a `ready`/`seated` entry into a decision). |
| `decision_engine_test.rb` uses a plain in-memory `FakeEntry` struct throughout, never a persisted `QueueEntry`, and touches no database anywhere in that file | Interpreted the prompt's own "the engine should be straightforward to unit test without a database when possible" (§24) as an instruction to actually demonstrate that, not merely satisfy it incidentally | Using real `ActiveRecord` `QueueEntry` rows for these tests would still have passed, but would leave the "pure, no DB" claim resting only on code inspection rather than on the tests themselves proving it — this way, the test suite's own structure is direct evidence of the boundary being real, and it's also simply faster. |
| The same tiny `FakeEntry` struct is duplicated (not shared via a cross-file constant reference) between `decision_engine_test.rb` and `allocation_algorithm_examples_test.rb` | Deliberate, not an oversight | An early draft referenced `DecisionEngineTest::FakeEntry` directly from the second file; caught before running that this is fragile, since Rails' test file loading order is alphabetical and `allocation_algorithm_examples_test.rb` would load *before* `decision_engine_test.rb`, raising `NameError` at class-body-evaluation time. A few duplicated lines in a second file were judged clearly preferable to a load-order-dependent cross-file constant reference. |
| `Allocation::ConfigurationGenerator` relies on `TableAdjacency`'s existing canonical-pair storage for deduplication, adding no dedup logic of its own | Recognized during implementation, not a new mechanism introduced | Phase 5B.2 already guarantees (via a DB check constraint, `table_id < adjacent_table_id`) that a pair is representable by exactly one row — so "never generate a reverse-duplicate pair" (this phase's own §8) falls out of that existing guarantee for free; adding a second deduplication mechanism here would have been redundant defensive code against a case the schema already makes impossible. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` or a `ai-corrections.md` entry — all are ordinary implementation-level decisions within the latitude this phase's own prompt explicitly granted for anything that doesn't change the locked algorithm itself (§3's "do NOT change the algorithm while implementing it" was honored throughout; every formula, weight, and tie-break rule was implemented exactly as `allocation-algorithm.md` specifies, verified by the fact that all 12 of that document's worked examples — 7 translated directly into tests — passed without any expected-outcome adjustment).

## Contradictions checked for — Session 15

- Compared every implemented formula (`fit_score`, `scarcity_score`, `aging_score`, `total_score`, the starvation pool override, the tie-break hierarchy) line-by-line against `allocation-algorithm.md` §6–§14 while writing `DecisionEngine` — no divergence found or introduced.
- Ran all 12 of `allocation-algorithm.md` §21's worked examples mentally against the implementation before writing tests for 7 of them (the remainder were judged adequately covered by the more granular §25-named unit tests already written, e.g. Examples 1/2/5/8 map directly onto the exact-fit/larger-table/starvation-buildup tests) — found no case where the specification's stated winner differed from what the code would actually produce.
- Confirmed via `git status` that no file outside `backend/app/services/allocation/`, `backend/test/services/allocation/`, and the one documentation file was touched, before writing any AI-working-record entries — satisfying this phase's own strict scope-verification requirement ahead of, not just inside, the final report.

No contradictions found requiring escalation.

## Session 16 (Phase 5B.5.3 — transactional allocation + atomic table reservation) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| `Allocation::SeatingCodeGenerator` implemented as a 6-character `SecureRandom`-backed code (32-symbol unambiguous alphabet), OPEN-005 left explicitly open | Direct application of this phase's own §15 permission ("smallest safe placeholder... record the decision... do not silently turn OPEN-005 into a new product decision") | The seating-code *format* is a product decision (staff-facing UX: how many characters, spoken aloud vs. typed, etc.) that hasn't been made — implementing something functionally correct (unique, server-generated, not DB-id-derived) without pretending to have resolved that product question. `decision-log.md`'s OPEN-005 row was deliberately left unedited. |
| Locking acquired on `Table` rows (`SELECT ... FOR UPDATE`), not on `SeatingAssignmentTable` rows | The natural candidate given `SeatingAssignmentTable` rows for a not-yet-claimed table don't exist yet to lock | `Table` rows are the one thing guaranteed to exist for every candidate table before any claim is made — locking them serializes any two transactions targeting the same table_id (both must acquire the same `Table` row's lock before either can safely read-then-write the claim state), which is exactly the mechanism §7/§17 describe. Locking `SeatingAssignmentTable` instead wouldn't work for the common case (no such row exists yet for a free table) and would require inventing a different mechanism for "reserve a table with zero existing claim rows." |
| Seating-code collision retried via a nested `ActiveRecord::Base.transaction(requires_new: true)` savepoint, not by retrying the whole outer allocation transaction | Recognized the same `PG::InFailedSqlTransaction` risk this project already hit once before (CORR-004's Phase 5B.2 Rails-console verification work) | A failed `UNIQUE` violation on `seating_code` would otherwise poison the entire enclosing transaction in PostgreSQL, forcing a full rollback (losing the already-created `SeatingAssignment`/`SeatingAssignmentTable` rows) on every collision, however rare. A real `SAVEPOINT` isolates just the failed code-assignment attempt, letting the rest of the reservation stand while only that one step retries — directly matches this phase's own §15 instruction to "retry generation inside the transaction," read literally. |
| `entry.status == "waiting"` re-check happens *after* the table lock(s) are acquired, not before | Ordering choice within the transaction body | Acquiring the table lock(s) first is what actually serializes competing transactions in the first place — checking entry status first wouldn't prevent two transactions from both passing that check and then racing on the table lock anyway. Locking the scarcer, contended resource (the table) before the cheaper, per-entry check is both correct and slightly more efficient (an entry that raced away from `waiting` for an unrelated reason, e.g. a hypothetical future `leave` action, is caught either way, but the table lock is the one that actually has to serialize concurrent callers). |
| `Allocation::ConfigurationGenerator`'s `Table.all` changed to `Table.order(:id)` | A determinism-hardening fix made while diagnosing the concurrency test failures below, not a response to any observed incorrect *allocation* decision | `Table.all` has no guaranteed row order without an explicit `ORDER BY`; this phase's own §4 requires "the same logical input state should produce the same decision," and `DecisionEngine`'s tie-break (§14) can only be as deterministic as the *input* candidate ordering it's given — an unordered read is a latent gap against that guarantee even though no concrete wrong decision was ever traced to it. Not logged as a full `ai-corrections.md` entry since it never actually produced an observably wrong result — a proactive hardening, not a caught defect. |
| `ReservationServiceConcurrencyTest`'s `teardown` corrected to restore an **empty** `Table`/`TableAdjacency` state, not to reseed | See `ai-corrections.md` CORR-007 for the full account | A genuine, real test-infrastructure bug self-caught via the test suite's own behavior (a previously-passing, unrelated test file starting to fail with a real `PG::ForeignKeyViolation`) — recorded as a correction, not a plain decision, but cross-referenced here since it also settled the general question of "what should a non-transactional allocation test class do to shared seed-adjacent tables in `teardown`" for any future test file in this project that needs the same `use_transactional_tests = false` pattern: restore whatever the *real, persisted* baseline is (empty, for `Table`/`TableAdjacency` in `test`), never assume the seed set is that baseline just because it commonly is in `development`. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the seating-code and locking-design items are ordinary implementation-level decisions this phase's own prompt explicitly delegated (§1's general framing plus §15's specific OPEN-005 permission), and the two fixes (ConfigurationGenerator ordering, concurrency-test teardown) were self-caught, self-corrected engineering issues, not product-decision or architecture-boundary questions.

## Contradictions checked for — Session 16

- Checked every item in the governing prompt's own §28 "important correction rule" trip-wire list (`Table.status`, locking only one table of a combined candidate, creating the assignment outside the transaction, skipping the post-lock availability check, silently choosing another candidate after a race, `QueueEntry waiting → seated`) against the actual implementation while writing it — none of these patterns appear anywhere in `Allocation::ReservationService`.
- Checked that `SeatingAssignment.create!(status: "pending")` never becomes `"active"` anywhere in this service — confirmed; `"active"` only ever appears in `allocation-spec.md` §5a's (unimplemented, unchanged) staff-confirmation description.
- Checked that no code path in this phase calls `Allocation::ReservationService` from `Guest::JoinService`, `Guest::CurrentQueueStatusService`, or any controller/route — confirmed via `git status`/`git diff` review before committing; zero files outside `backend/app/services/allocation/` (plus its own tests and three documentation files) were touched.

No contradictions found requiring escalation beyond CORR-007, already corrected and recorded.

## Session 17 (Phase 5B.5.4 — allocation trigger integration + repeated allocation orchestrator) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| `now` captured once per `Orchestrator.call` and reused for every pass within that run, not re-read via `Time.current` per pass | Direct response to this phase's own §8 ("if the allocation operation takes significant time, inspect the existing policy before deciding whether now should remain fixed... Do NOT invent a new starvation policy") | A single orchestration run is expected to complete in a handful of fast, purely-DB-round-trip passes (no external I/O) — freezing `now` avoids a group's aging/starvation status flipping mid-run purely from wall-clock drift, which would be a confusing, non-reproducible artifact nothing in `seating-allocation-policy.md`/`starvation-policy.md` asks for. This mirrors the same "inject `now`, don't call `Time.current` deep inside scoring" discipline `DecisionEngine` already established (Phase 5B.5.2 §29) — extending it to the orchestration boundary rather than just the pure engine. Not a new starvation *policy* — the threshold/derivation logic is completely unchanged; only the wall-clock reference point used across one orchestration run is fixed. |
| Release and Guest Leave triggers reported as deferred, not integrated | Direct application of this phase's own §14/§16/§31/§33 instructions | Neither has any implemented code path anywhere in this codebase — no staff release endpoint/service exists (`api-spec.md`'s `POST /staff/seating-assignments/release` is specification-only), and `POST /guest/queue-entries/current/leave` has never been built (deferred every phase since 5B.3). This phase authorizes *trigger integration*, not new guest/staff-facing API surface — building either endpoint now would be scope creep into a future phase's authorized work, exactly the case §16 anticipates ("do not invent a new leave API if it has not yet been authorized... report it as deferred"). |
| Guest Join's `201` response now returns the entry's live `status` (which may be `"ready"`), not a value hardcoded to `"waiting"` | Judged as a faithful reading of this phase's own §13 ("do NOT change the response contract simply because allocation may now happen... if a new join can synchronously become READY, verify what the approved API contract requires") | `api-spec.md`'s join response shape (`{ entry_id, active_visit_token, position, status }`) already carries an unconstrained `status` field — its illustrative `"waiting"` example predates allocation existing at all and was never a promise that `status` could only ever be `"waiting"` there. The controller's `entry_json` method already rendered `entry.status` dynamically (never hardcoded), so no code change was needed there — only documenting, in `api-spec.md` itself, that the value can now honestly differ from the example. Judged NOT a "genuine specification conflict" requiring `BLOCKED` escalation (§13's own STOP condition): the response *shape* is unchanged, and no other document asserts `status` must remain `"waiting"` at this endpoint. |
| `MAX_CONSECUTIVE_STALE_RESULTS = 5`, resetting to zero on every real success | This phase's own §19 explicit instruction: "a reasonable safeguard is a bounded retry count for concurrency-loss cases, but do not introduce arbitrary limits that prevent normal multi-allocation behavior" | Resetting on success (rather than a single global cap across the whole run) is what actually satisfies "does not prevent normal multi-allocation behavior" — a long, fully successful run of 50 allocations never trips the bound, while 5 *consecutive* losses (only plausible under genuine external contention, since a single-process sequential loop can't race itself) stops a busy-loop. Labeled a tunable defensive constant, not a business rule, matching the project's established `WEIGHT_*`/`STARVATION_THRESHOLD_SECONDS` labeling convention. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the `now`-freezing and stale-retry-bound items are ordinary implementation-level decisions this phase's own prompt explicitly delegated (§8, §19), and the Release/Guest-Leave deferral and join-response items are direct, unambiguous applications of the prompt's own explicit instructions, not free interpretive choices made without textual grounding.

## Contradictions checked for — Session 17

- Checked every item in the governing prompt's own §37 "important correction rule" trip-wire list (caching candidate scores across passes, allocating by `joined_at` order instead of `DecisionEngine`, bypassing `ReservationService`, table locking in the orchestrator, adding Sidekiq/Redis, a background READY-expiration sweep, silently changing the API contract) against the actual implementation — none of these patterns appear anywhere in `Allocation::Orchestrator` or the two trigger call-sites.
- Checked that `DecisionEngine`'s formulas and `ReservationService`'s transaction logic were not touched, moved, or duplicated anywhere in this session's diff — confirmed via direct review before committing; both files are untouched in `git diff`.
- Checked that the lazy-expiration trigger only ever fires the orchestrator when `expire_if_overdue` actually performed an expiration (not on every `ready`-entry read) — verified both by code review and by the dedicated "reading a ready entry that is NOT yet overdue never invokes allocation" test.

No contradictions found requiring escalation.

## Session 18 (Phase 5B.6 — staff seating confirmation) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| No staff authentication implemented; `POST /staff/seat` is fully unauthenticated | Direct application of this phase's own §15 ("do NOT create a complete authentication system... use the existing documented authentication boundary or explicitly report that authentication remains deferred") | There is no "existing documented authentication boundary" to use — `StaffUser` has `has_secure_password` but no session/login endpoint exists anywhere in this codebase (deferred every phase since 5B.2). The honest choice was to implement the real business logic fully and correctly while leaving the endpoint openly, visibly unauthenticated and calling that out explicitly in three places (`api-spec.md`, `traceability.md`, this record) — rather than either building a throwaway auth stub (which §2 explicitly forbids: "do NOT build a complete authentication subsystem") or silently shipping an unauthenticated endpoint without flagging it (which §15's last line, "do not weaken the domain logic just because authentication is deferred," and general project transparency norms, both argue against). |
| `left`/`no_show` codes classified as `conflict`, not `not_found` — resolving a real contradiction in `api-spec.md` itself | See `ai-corrections.md` CORR-008 for the full account | A genuine, real specification internal-inconsistency self-caught while implementing, not a judgment call made from nothing — recorded as a correction, cross-referenced here since it directly shaped `Staff::ConfirmSeatingService#classify`'s design. |
| DEC-015 lazy-expiration check added to staff confirmation itself, duplicated (not extracted) from `Guest::CurrentQueueStatusService` | See `ai-corrections.md` CORR-009 for the full account | A genuine correctness gap relative to an already-approved specification (`functional-spec.md` §6a step 2), self-caught — recorded as a correction. The duplication-vs-extraction choice itself: two call sites was judged not yet enough to justify a shared abstraction (CLAUDE.md's "prefer small changes," and the project's established practice of not adding structure ahead of a genuine third need) — flagged explicitly in both services' comments as a deliberate, revisit-later choice rather than an unexamined one. |
| The DEC-015 expiration branch inside `ConfirmSeatingService` lets its state changes **commit** even though the confirmation itself is rejected — asymmetric with every other rejected-confirmation branch, which rolls back | Deliberate, reasoned exception to the otherwise-uniform "reject → `ActiveRecord::Rollback`" pattern | A rejected confirmation attempt (wrong code, already seated, etc.) changes nothing and should roll back cleanly. A genuine DEC-015 expiration discovered *during* a confirmation attempt is not "nothing happened" — it's a real, independently-meaningful state transition (the same one `Guest::CurrentQueueStatusService` would perform on any other read) that must persist regardless of whether the confirmation attempt that happened to trigger it succeeds. Rolling it back would silently undo a real expiration merely because staff's timing was unlucky — worse than either committing it or not checking at all. |
| The table freed by a confirmation-triggered expiration is deliberately *not* passed to `Allocation::Orchestrator`, even though "READY expiration" is an approved allocation trigger (Phase 5B.5.4) | Direct, absolute application of this phase's own §25 ("Staff confirmation must NOT call... Allocation::Orchestrator... Confirmation does not allocate") — no exception carved out for this edge case | This creates a real, narrow, honestly-documented gap: a table freed this specific way stays free until some *other* operation (a future join, a future guest status read) picks it up, rather than being reallocated in the same request. Judged NOT worth escalating to `BLOCKED`, since §25's prohibition is unconditional and unambiguous — extending the no-orchestrator-call rule to this one additional branch is the only reading consistent with the phase's own explicit boundary, not an invented exception to it. |
| Lock order fixed as `QueueEntry` (the `seating_code` lookup key) then its own `SeatingAssignment` | Not really a "choice" among alternatives — there is exactly one relationship path to traverse here | Unlike `ReservationService`'s interchangeable table set (which genuinely needs a "sort by id" rule to pick among several equally-valid orderings), a `QueueEntry` has exactly one `current_seating_assignment` — there is no second valid order to consider, so no explicit ordering *policy* was needed beyond "look up the entry, then its assignment," which is also the natural order the lookup itself requires. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the authentication and no-orchestrator-call items are direct, unambiguous applications of this phase's own explicit instructions, and the two specification/implementation gaps (CORR-008, CORR-009) were genuine corrections, not open questions needing a human's product judgment.

## Contradictions checked for — Session 18

- Checked every item in the governing prompt's own §31 "important correction rule" trip-wire list (creating a new `SeatingAssignment`/`SeatingAssignmentTable` during confirmation, allocating another table, changing `Table.status`, generating a new `seating_code`, calling `Allocation::Orchestrator`, adding Sidekiq/Redis, silently building staff authentication) against the actual implementation — none of these patterns appear anywhere in `Staff::ConfirmSeatingService`.
- Re-read `api-spec.md`'s `POST /staff/seat` section end-to-end against the implementation before considering the phase done — this is what surfaced CORR-008.
- Re-read `functional-spec.md` §6a end-to-end (not just the sections the initial implementation already satisfied) while writing this session's documentation update — this is what surfaced CORR-009.
- Confirmed via `git status`/`git diff` review before committing that no file outside `backend/app/services/staff/`, `backend/app/controllers/staff/`, `backend/config/routes.rb`, their tests, and the two documentation files was touched.

No contradictions found requiring escalation beyond CORR-008 and CORR-009, already corrected and recorded.

## Session 19 (P0 completion mode — Guest Join frontend vertical slice) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Real browser verification run from a separate, throwaway `--network host` Docker container, not inside the frontend container itself | Discovered mid-session: a headless browser launched *inside* the frontend container got `ERR_CONNECTION_REFUSED` against `http://localhost:3000`, because that container's own "localhost" is a different network namespace than the one `VITE_BACKEND_URL=http://localhost:3000` (set in `docker-compose.yml`) is designed for — a real developer's browser on the host machine, where Docker's port mappings make that URL resolve correctly | This is not a bug in the app or its configuration — `VITE_BACKEND_URL`'s value is correct for its actual purpose (a real end-user's browser). The verification tooling had to match that same vantage point to be a valid test, so the headless browser was run in a container with `--network host`, which resolves "localhost" the way the Colima Linux VM (where the port mappings actually terminate) does — confirmed first with a plain `curl` check before trusting it for the full Playwright run. |
| `playwright` installed via `npm install --no-save` into the frontend's existing named `node_modules` volume, reused by the verification container, rather than added to `package.json`/`package-lock.json` | Direct application of the governing prompt's own "do not introduce unnecessary libraries" instruction | The verification tooling itself isn't a project dependency — `--no-save` guarantees `package.json`/`package-lock.json` are never touched, and the temporary script that used it was deleted before any `git add`. Confirmed via `git status` that neither manifest file appears as changed. |
| Demonstrated the WAITING state with `group_size: 100`, not by artificially pre-occupying the seed tables | A group size no current or realistically-future table configuration can ever satisfy (max combined capacity is 4+4=8) reliably stays `waiting` indefinitely, with zero setup | Simpler and more honest than temporarily claiming/occupying real seed tables just to force a `waiting` result for the demo, and it doesn't risk leaving the seed data in a weird intermediate state if cleanup were interrupted. (Note: this only "works" because DEC-011's oversized-group rejection at join time is still deferred, Phase 5B.3 — a real join with `group_size: 100` is accepted rather than rejected; documented here so the choice doesn't read as accidentally relying on unfinished validation without acknowledging it.) |
| The join-response follow-up call to `GET /guest/queue-entries/current` is unconditional (always made after every successful join), not only when `status === "waiting"` | The join response's `status` field alone doesn't carry `position` or `seating_code` in either case — both require the second call regardless of which status came back | Making the second call conditional on `status` would have meant writing two different code paths that converge on the same "render the true current state" outcome; a single unconditional follow-up is simpler and can never render a stale/guessed value for either state. |
| The pre-existing `h1` heading-overlap (from `frontend/src/index.css`'s pre-existing `font-size: 56px` rule, combined with "Restaurant Waitlist" wrapping at this viewport width) was left unfixed after removing this session's own contributing `maxWidth` | The governing prompt's explicit "No visual polish is required," plus the fact that this exact heading text and global CSS rule already existed in the codebase before this session — not a regression this task introduced | Distinguished explicitly from the `maxWidth: 360` issue (which *was* fixed): that constraint was newly introduced by this session's own code, so leaving it would have been a self-caused, unreported defect; the remaining overlap is inherited template styling, out of this task's stated scope (build the join screen, reuse existing conventions — not audit/fix the template's global CSS), and reported transparently in the final report rather than silently left for a future session to rediscover. |
| No GitHub remote configured — the push step (§12) could not be performed | `git remote -v` returned empty | Reported honestly in the final report rather than fabricated or silently skipped; not something to resolve unilaterally (adding/configuring a remote is a repository-identity decision for the user, not an implementation detail this task's scope covers). |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — all are ordinary implementation/verification-tooling decisions within this task's own stated latitude ("use existing conventions," "do not introduce unnecessary libraries," "no visual polish required"), and the missing-remote finding is a factual report, not a judgment call needing a human decision to proceed with the rest of the task.

## Contradictions checked for — Session 19

- Checked the actual `POST /guest/queue-entries` and `GET /guest/queue-entries/current` response shapes directly against the already-implemented `Guest::JoinService`/`Guest::CurrentQueueStatusService`/`Guest::QueueEntriesController` source (not just `api-spec.md`'s prose) before writing the frontend's TypeScript types, to avoid the frontend silently assuming a field (e.g. `position` on the join response) that the backend doesn't actually return.
- Confirmed via `git status`/`git diff` review before committing that no file outside `frontend/src/App.tsx` and the one documentation file was touched — in particular, confirmed no backend file, migration, or `Allocation::*`/`Staff::*` service was modified, per this task's explicit §5 boundary.
- Confirmed the temporary verification script and screenshots (`frontend/e2e_verify.mjs`, `frontend/e2e-*.png`) were deleted and absent from `git status` before staging anything.

No contradictions found requiring escalation.

## Session 21 (P0 task — Staff Login stub) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Signed `ActiveSupport::MessageVerifier` bearer token, not JWT/a new DB session table | Direct application of this task's own §2 ("do NOT introduce JWT/session infrastructure unless the authoritative specification explicitly requires it") | A built-in Rails primitive already present in this codebase's dependency graph — zero new gems, no new migration/table. `api-spec.md` itself left the mechanism "TBD (e.g., cookie or bearer token)," so choosing a concrete mechanism was this task's own authorized work, not an invented requirement. Bearer-token transport specifically (over a cookie) was chosen to match the project's already-established `Authorization: Bearer <token>` convention used for guest `active_visit_token`s, keeping the two authentication surfaces structurally parallel in *shape* while remaining verification-mechanism-distinct (see next row). |
| Guest and staff tokens kept structurally separate — a signed payload vs. a random DB-verified value, not just two different tables | Direct application of this task's own §4 ("a guest `active_visit_token` must never authenticate a staff user") | Using a different verification *mechanism* entirely (signature check vs. DB lookup), not merely a different table with the same lookup shape, makes a guest token structurally unable to ever pass staff verification — it isn't a validly-signed payload at all, so there's no code path where a future refactor could accidentally blur the two. Verified directly with a dedicated test (`login_controller_test.rb`, `seat_controller_test.rb`) rather than left as an assumption. |
| `POST /staff/seat` now inherits `Staff::BaseController` and requires authentication | Cross-referenced two textual signals: `api-spec.md`'s own pattern (`/staff/queue` and `/staff/tables` both say "Auth: staff session"; `/staff/seat` conspicuously did not) and Session 18's own record explicitly stating enforcement was "deferred to whichever future phase builds `POST /staff/login`" | This task's own §5 authorizes exactly this: "if the authoritative specification says the login mechanism must protect it now, apply the minimum required check." Session 18's own documentation is that authoritative textual grounding — this phase is the one it named, so applying the check is a textually-supported reading, not an invented product decision. No other Staff endpoint was touched, per §5's explicit "do not implement any other Staff endpoint." |
| One demo `StaffUser` seeded (`staff@example.com` / an obviously-fake, documented password) and committed in `db/seeds.rb` | This task's own §3: "If the specification defines a stub credential or seeded staff user, follow it exactly; if it does not, do not silently invent one" — no `StaffUser` row existed anywhere and the login endpoint is otherwise untestable without one | Judged safe to commit specifically because it is categorically the same as this project's already-committed `database.yml` dev defaults (`postgres`/`postgres`) — an obviously-fake, explicitly-documented local-dev-only value, not a credential for any real system. Given the repository is now public, this distinction was checked deliberately rather than assumed: the value is inert (protects nothing beyond a P0 stub with no real data behind it) and its fakeness is stated directly in the seed file's own comment, so it is not a "secret" in the sense §10's secret-safety check is guarding against. |
| A minimal Staff Login screen was implemented in the frontend | `functional-requirements.md` REQ-FE-002 ("Both guest and staff screens implement the flows above") plus this task's own §6 explicit UI mockup | Read as in-scope for P0 specifically because it is named directly in an already-approved requirement, not inferred from the prompt's UI mockup alone — the mockup only specified *how minimal*, not *whether*. All other staff UI (queue view, table view, release, no-show) remains explicitly out of scope per §6's own list, and no new frontend library/router/state-management dependency was added. |
| The 4 pre-existing `QueueEntry` rows (ids 27–30, phone `9877447351`) found in the dev database were left untouched | Not this session's data — they don't match this or any prior session's own `555-XXXX` disposable-test-data convention, and appear to be the user's own manual testing between sessions | Deleting data this session didn't create, without being asked, would risk destroying real in-progress work — the same caution this project applies to git history/branches. `Table.count == 40` was checked directly and confirms the seed baseline itself is unaffected regardless of these rows' presence, so nothing about verification depended on removing them. Reported to the user directly rather than silently left for them to notice later. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the token-mechanism and `/staff/seat`-authentication items are direct, textually-grounded applications of this task's own explicit instructions (§2, §5) combined with this project's own prior session record, the demo-credential and Staff-Login-screen items are ordinary implementation-level judgment calls within this task's own stated latitude (§3, §6), and the pre-existing-rows finding is a factual report/conservative-default, not a product decision needing a human's judgment to proceed.

## Contradictions checked for — Session 21

- Checked every item in the governing prompt's own "strict scope — do not implement" list (Staff Queue, Staff Table View, Staff No-show, Staff Release, staff registration, password reset, OAuth, SSO, MFA, Redis, Sidekiq, WebSockets, notifications, live updates, rate limiting, analytics, P1 functionality, allocation algorithm changes, domain model redesign) against the actual implementation — none of these patterns appear anywhere in this session's diff.
- Verified the repository's public/pushed status independently (`git fetch origin` + `git log origin/main --oneline` compared to local `main`) rather than trusting the governing prompt's own claim, before proceeding with the heightened §10 secret-safety diligence it calls for.
- Ran the exact §10 secret-safety commands (`git status`; `git ls-files | grep -Ei '(^|/)\.env($|\.)|master\.key|\.pem$|\.key$'`; `git check-ignore -v backend/config/master.key`) and reviewed their output directly before staging anything — confirmed no `.env`/`master.key`/`.pem`/`.key` file is tracked or staged, and that `.gitignore` was not modified.
- Confirmed via `git status`/`git diff --stat`/`git diff` review before committing that no file outside the Staff-login-specific backend files, `frontend/src/App.tsx`, their tests, `db/seeds.rb`, and two documentation files was touched — in particular, confirmed `Allocation::*`, `Guest::*`, and every other Staff-related file beyond `SeatController`'s superclass line remain untouched.

No contradictions found requiring escalation.

## Session 22 (P0 task — Staff Queue vertical slice) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| DEC-015 lazy-expiration logic extracted into a shared `Allocation::LazyExpiration` module, refactoring `Guest::CurrentQueueStatusService` and `Staff::ConfirmSeatingService` to use it | Direct application of this task's own §6 ("reuse the existing expiration mechanism... do not create a second expiration implementation"), combined with `Staff::ConfirmSeatingService`'s own pre-existing comment explicitly flagging "a third call site would tip this toward extraction" | `Staff::QueueViewService` is that third call site. Duplicating the same six-line check a third time would have directly contradicted this task's own explicit instruction; extracting it is the only reading consistent with both that instruction and the codebase's own already-stated intent. Verified the refactor changed no observable behavior by running the full pre-existing test suite (unedited) and confirming it stayed green. |
| A Staff Queue read never calls `Allocation::Orchestrator`, even when it expires an overdue `ready` entry as a side effect | This task's own §6/§7: "A Staff Queue read must not perform new allocation" — unconditional, no exception carved out | Directly mirrors the precedent `Staff::ConfirmSeatingService` already established for the identical situation (Session 18: its own governing phase forbade calling `Orchestrator` from its expiration branch too, even though `Guest::CurrentQueueStatusService`'s equivalent branch does call it). The freed table isn't lost — it stays free until a future join or guest status read (both already-approved allocation triggers) picks it up, exactly as DEC-015's "lazy, no delivery guarantee" model already allows. Not treated as a genuine specification conflict requiring `BLOCKED` escalation: both this task's instruction and the prior precedent point the same direction. |
| Response envelope `{ waiting: [...], ready: [...] }`, waiting ordered by `joined_at` ascending, ready ordered by `ready_at` ascending | `api-spec.md`'s existing `GET /staff/queue` section specified the two per-entry field sets but not an exact JSON envelope or explicit ordering rule | Judged as the most direct, literal reading of the existing prose ("list of waiting entries... and ready entries...") rather than inventing additional structure (e.g., a single merged/flagged list, or a differently-named envelope). Waiting ordering matches the existing chronological `position` computation exactly (so the list order and the `position` numbers agree, avoiding a confusing mismatch); ready ordering has no existing precedent to match, so a simple deterministic default (`ready_at` ascending) was chosen and documented explicitly as a judgment call rather than left implicit. |
| `is_starvation_protected` computed via a direct, duplicated two-line comparison against `Allocation::Policy::STARVATION_THRESHOLD_SECONDS`, not by calling `Allocation::DecisionEngine`'s private `starvation_protected?` | This task's own §5: "do not duplicate the allocation algorithm... queue position is not allocation priority" | Referencing the existing shared constant (rather than hardcoding the threshold value again) avoids the one thing actually worth avoiding — silent drift between this flag and the real allocation algorithm's own threshold — while the comparison itself is a single boolean expression, not a reimplementation of any scoring/weighting/tie-breaking logic. Consistent with this codebase's own established tolerance for small, informational-only duplication (e.g., `Guest::CurrentQueueStatusService`'s own `position_for` before this task, `CurrentQueueStatusService`/`ConfirmSeatingService`'s now-extracted DEC-015 check before it hit a third site). |
| The 4 pre-existing `QueueEntry` rows (ids 27–30) were left untouched again this session | Same reasoning as Session 21 — not this session's data, and `Table.count`/`TableAdjacency.count` checks don't depend on their removal | One of them (id 29, `waiting`, `group_size: 20`) surfaced directly in this session's own real-HTTP and real-browser verification output (correctly shown as `is_starvation_protected: true`) — left in place and used as-is for verification rather than worked around, since it's real data the endpoint is supposed to handle correctly regardless of who created it. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — the extraction and no-Orchestrator-call items are direct, textually-grounded applications of this task's own explicit instructions plus this project's own prior precedent (Session 18), and the envelope/ordering/starvation-flag items are ordinary implementation-level judgment calls within this task's own stated latitude, each documented explicitly rather than left implicit.

## Contradictions checked for — Session 22

- Checked every item in the governing prompt's own §14 "strict scope" list (Staff Table API/UI, Staff Release, Staff No-show, staff management, registration, roles/permissions, OAuth/SSO/MFA, password reset, Redis, Sidekiq, WebSockets, notifications, live updates, rate limiting, analytics, P1 fairness/missed-opportunity/fairness-debt/predictive-demand work, allocation-algorithm changes, domain redesign, unrelated frontend work) against the actual implementation — none of these patterns appear anywhere in this session's diff.
- Checked §7's "must not accidentally" list directly against `Staff::QueueViewService` — confirmed via a dedicated test (`no QueueEntry, SeatingAssignment, or SeatingAssignmentTable row is created by a plain read`, using `assert_no_difference`) rather than only by code inspection.
- Confirmed via `git status`/`git diff --stat`/`git diff` review before committing that no file outside the Staff-Queue-specific backend files, the two refactored existing services (behavior-preserving only), `frontend/src/App.tsx`, their tests, and two documentation files was touched — in particular, confirmed `Allocation::DecisionEngine`/`ReservationService`/`Orchestrator` themselves, and every Staff Table/Release/No-show-related file (none of which exist yet), remain untouched.

No contradictions found requiring escalation.

## Session 23 (P0 task — Frontend session persistence & recovery) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| Both `GuestJoin` and `StaffLogin` compute their initial screen phase via `useState`'s lazy initializer reading `localStorage` directly, rather than starting in a generic loading/init phase and transitioning after an effect | This task's own §10: "avoid rendering the wrong screen briefly... do not show Join Queue for a moment and then unexpectedly replace it" | A lazy initializer runs synchronously during the first render, before anything paints — there is no flash to avoid because the wrong screen is never rendered even for one frame, which is strictly better than an init-phase-then-effect approach for the part that's actually synchronous (reading storage). The genuinely asynchronous part (validating the guest token against the backend) still gets its own `'recovering'` phase, since that part cannot be resolved synchronously. |
| The restored Staff `'authenticated'` state is optimistic — no dedicated "validate this token" call is made before rendering it; `StaffQueue`'s own pre-existing `GET /staff/queue` fetch is treated as the real validation, with a new `401`-specific `onUnauthorized` path | This task's own §7 explicitly prescribes this exact flow ("restore the authenticated Staff state and allow the Staff UI to load. The Staff Queue should then call the existing `GET /staff/queue`...") and §17 forbids adding any new backend API — there is no staff "whoami" endpoint to call instead | Not a shortcut around "do not fabricate an authenticated state without validating the token" (§7's own other explicit instruction) — the validation still happens, on the very next real network call the authenticated screen makes, and a `401` there is treated with exactly the weight §7 demands (clear + return to Login). Only a genuinely idle authenticated screen that never calls the queue would go unvalidated, which doesn't happen since `StaffQueue` always renders and fetches immediately. |
| Only an HTTP `401` from `GET /staff/queue` clears the stored Staff session; any other failure (thrown network exception, a non-401 error status) leaves it untouched | This task's own §7: "Do not treat a temporary network error as proof the token is invalid," mirrored for Guest by §4's "do NOT immediately delete a valid stored token merely because of a temporary network error" | `401` specifically is `Staff::BaseController`'s own unambiguous "this token does not authenticate" signal (Session 21) — nothing else reliably means that. Same reasoning applied symmetrically to the Guest side: only a `404` from `GET /guest/queue-entries/current` (the endpoint's own documented "no active visit" signal) clears `guest_active_visit_token`; every other failure enters a distinct `'recovery-error'` phase with a token-preserving Retry button instead. |
| A companion, non-sensitive `staff_session_email` storage key was added alongside `staff_session_token`, purely for display | Not explicitly requested by the prompt, but not forbidden either — email is not a credential (it's typed into a public login form) | Chosen so "Logged in as `<email>`" continues to render correctly after a refresh without needing a whoami endpoint (forbidden by §17) or hardcoding a placeholder. Judged in-scope as a minimal, load-bearing detail of restoring the *existing* authenticated UI faithfully (§7's own framing: "restore the authenticated Staff state"), not a new feature — if this judgment call had been wrong, the fallback (omitting the email, showing just "Logged in.") would have cost nothing, so the risk of overreach was low. |
| The pre-existing "Start Over" button in `GuestJoin` was deliberately left unchanged — it does not clear `localStorage` | This task's own §5: "Terminal state: follow the existing product behavior... do not invent a new guest-session lifecycle," combined with the fact this button predates this task and was never in its explicit Join/Leave lifecycle list | "Start Over" has never called the real Leave API — it only resets local UI state. If it also cleared the stored token, a guest with a genuinely still-active `waiting`/`ready` backend visit who clicked it would find that visit silently un-recoverable on their next refresh, even though nothing about their real reservation changed — a strictly worse outcome than leaving it alone, and not something this task's own explicit lifecycle rules (Join/idempotent-retry/Leave/terminal) asked for. Documented directly in the code, not just here, so a future session doesn't mistake the omission for an oversight. |
| The Guest/Staff tab-toggle selection itself (`App()`'s own `view` state) was not persisted | This task's own §9 requirement is that switching tabs "must not delete" the other side's session/visit — not that the currently-open tab itself survives a full page refresh | Verified structurally rather than assumed: since `GuestJoin`/`StaffLogin` each read their own state from `localStorage` via a lazy initializer on every mount, unmounting one when the tab switches (React's normal conditional-render behavior) never touches `localStorage`, so remounting later reconstructs identical state — §9 is satisfied without lifting any state into `App()` or adding a state-management library, honoring §10's "keep it simple... do not introduce a global state framework" instruction. A page *refresh* (as opposed to a tab switch) always lands back on the default Guest tab, which is a distinct, allowed behavior the prompt never asked to change. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — every item above is a direct, textually-grounded application of this task's own explicit instructions (§4, §5, §7, §9, §10, §17), each documented explicitly rather than left implicit.

## Contradictions checked for — Session 23

- Checked the prompt's own §15 "do not change backend contracts" list item by item against the actual diff — confirmed via `git diff --stat` that no file under `backend/` was touched at all this session; every persistence/recovery mechanism was built entirely from the existing, unmodified `GET /guest/queue-entries/current`, `POST /guest/queue-entries/current/leave`, `POST /staff/login`, and `GET /staff/queue` contracts.
- Checked §17's strict "do NOT implement" list (Staff Table/Release/No-show, OAuth/SSO/MFA, refresh-token infrastructure, Redis/Sidekiq/WebSockets, new backend APIs, unrelated frontend redesign) against the actual diff — none of these patterns appear; in particular, no whoami/session-introspection endpoint was added despite the Staff-recovery flow's own natural pull toward wanting one (resolved instead via the optimistic-restore-plus-real-fetch-validates design above).
- Checked §11's "do not expose tokens" instruction directly against this session's own verification script and this record — confirmed no real token value appears in either; Flow E's garbage-token injections used literal placeholder strings (`'garbage-guest-token'`, `'garbage-staff-token'`), never a real issued token.
- Confirmed via `git status`/`git diff --stat` review before committing that the diff touches only `frontend/src/App.tsx` and the two documentation files.

No contradictions found requiring escalation.

## Session 20 (P0 task — Guest Leave) — decisions made

| Item | What happened | Reasoning |
|---|---|---|
| `Allocation::Orchestrator` triggered only when leave actually released a table (not unconditionally on every leave) | Direct application of this task's own Step 4 framing ("if Guest Leave releases a table reservation, trigger the existing allocation mechanism") plus consistency with the already-established conditional lazy no-show trigger (Phase 5B.5.4) | A `waiting` guest leaving removes one *candidate* from the pool but can never free a table or create a new allocation opportunity from nothing — the orchestrator would only ever return `:no_candidate` for that case, so calling it is a guaranteed no-op. Mirroring the existing `if expired` pattern from `Guest::CurrentQueueStatusService` rather than introducing a second, inconsistent (unconditional) idiom for the same kind of trigger. |
| Leaving an already-terminal entry (`left`/`seated`/`no_show`) reports that entry's **real** current status, not a forced `"left"` | Resolved a real precision gap between `api-spec.md`'s shorter phrasing (`{status: "left"}`) and `functional-spec.md` §3's more careful one ("leaving an already-terminal entry is a safe no-op") | Forcing the response to always say `"left"` even for a guest who is actually `seated` would be a fabricated/misleading response — directly against this project's standing rule (established every prior phase) never to report a status the database doesn't actually hold. `functional-spec.md` is the more precise, more authoritative document on this exact point (it explicitly generalizes beyond "already left" to "any terminal state"), so its reading governed. Not logged as an `ai-corrections.md` entry — this was resolved during design, before any code was written to the wrong behavior, not caught after the fact. |
| No DEC-015 lazy-expiration re-check added inside `Guest::LeaveService` | `functional-spec.md` §3 does not name Guest Leave as a DEC-015 checkpoint, unlike §2 (`current`) and §6a (staff confirmation), which both explicitly do | Deliberately did not extend the pattern to a third call site without a textual basis for it — the two existing checkpoints are each explicitly written into the spec; leave is not. Functionally the outcome is equivalent either way (the table gets released correctly regardless of whether the terminal status ends up `left` or `no_show`), so the omission has no correctness cost, only a minor, explicitly-documented difference in which specific terminal status a guest sees if their reservation happened to already be overdue when they chose to leave. |
| Flow C (allocation-after-leave) verified with the database temporarily reduced to exactly one table, not the full 40-table seed | The seed's other 39 free tables would let a second guest also get seated immediately regardless of whether the first guest's leave-triggered allocation worked, making the demonstration ambiguous | Same isolation technique already established for backend test fixtures in earlier sessions (Phase 5B.5.x), applied here to a real-browser manual verification for the first time — temporarily wiped and restored via `bin/rails runner`, verified restored to the exact 40/19 baseline afterward. |
| `active_visit_token` promoted to persistent component state in the frontend (previously used once and discarded) | Needed by the new "Leave Queue" button, which fires after the join+status round trip that originally held the token only in a local variable | A minimal, load-bearing change directly required by this task's own Step 8 — not scope creep, since without it there would be no way to identify which visit to leave. Still never persisted beyond component state (no `localStorage`/cookies) — recovering a visit across a page reload remains explicitly out of scope, unchanged from Session 19. |

None of these required `BLOCKED — HUMAN DECISION REQUIRED` — all are ordinary implementation-level decisions within this task's own explicit instructions and this project's already-established patterns (conditional trigger idioms, honest-status-reporting, DEC-015-checkpoint-only-where-specified, isolated verification technique).

## Contradictions checked for — Session 20

- Compared `api-spec.md`'s leave section against `functional-spec.md` §3 line-by-line before implementing — found the idempotency-scope precision gap described above and resolved it in favor of the more precise document, documented in `api-spec.md` itself rather than left implicit.
- Checked whether Guest Leave should be a DEC-015 checkpoint by re-reading §2/§3/§6a of `functional-spec.md` side by side — confirmed §3 is silent on this while §2/§6a are explicit, and did not extend the pattern without textual grounding.
- Confirmed via `git status`/`git diff` review before committing that no file outside `backend/config/routes.rb`, `backend/app/services/guest/leave_service.rb`, `backend/app/controllers/guest/queue_entries_controller.rb`, `frontend/src/App.tsx`, their tests, and two documentation files was touched — in particular, confirmed `Allocation::DecisionEngine`/`ReservationService` and every staff-related file remain completely untouched, per this task's own scope guard.

No contradictions found requiring escalation.
